\section{Containerarchitektur [Leopold Mistelberger]}
\setauthor{Elias Mahr}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pics/ContainerArchitecture.png}
    \caption{Containerarchitektur}
    \label{fig:Containerarchitektur}
\end{figure}


Die Containerarchitektur beschreibt den Aufbau und die Organisation der technischen Komponenten eines Baustellencontainers.

\subsection{Zentrale Steuereinheit}

Die zentrale Steuerung erfolgt über einen \textbf{Raspberry Pi 5}, der als Edge-Gateway für Sensorik, Aktorik und Cloud-Schnittstelle dient. Auf dem Gerät laufen mehrere getrennte Containerdienste mit klarer Aufgabenverteilung:

\begin{itemize}
    \item \textbf{Zigbee2MQTT}: Übersetzt Zigbee-Gerätedaten in MQTT-Nachrichten und umgekehrt
    \item \textbf{MQTT-Broker (Mosquitto)}: Lokaler Message-Bus zwischen Diensten und Zigbee2MQTT
    \item \textbf{mqttListener}: Persistiert Sensorzustände strukturiert in Firestore
    \item \textbf{actionHandler}: Liest Steuerbefehle aus Firestore und schaltet Aktoren über MQTT
    \item \textbf{Home Assistant}: Lokale Automatisierung und Visualisierung
\end{itemize}

Die Broker-Konfiguration ist in \texttt{config.json} zentral definiert (Host \texttt{mosquitto}, Port \texttt{1883}). Dadurch greifen Listener und Action-Handler auf dieselbe Kommunikationsbasis zu. Administration und Wartung des Raspberry Pi erfolgen über SSH-Zugriff, wodurch Updates und Fehleranalyse ohne physischen Zugriff möglich sind.

Die Kommunikation zwischen Feldgeräten und Raspberry Pi erfolgt über ein lokales Zigbee-Mesh. Zigbee wurde im Projekt gewählt, weil das Protokoll auf geringe Leistungsaufnahme, Mesh-Routing und Interoperabilität zwischen Herstellern ausgelegt ist. Diese Eigenschaften sind für batteriebetriebene Sensoren in verteilten Containerinstallationen relevant \cite{csa_zigbee_overview}.


\subsection{Sensorik}

Zur Erfassung der Umgebungsbedingungen im Container sind verschiedene Sensoren installiert:

\begin{itemize}
    \item \textbf{Temperatur- und Luftfeuchtigkeitssensor (innen)}: Misst kontinuierlich die klimatischen Bedingungen im Containerinneren. Über ein integriertes Display haben die Mitarbeiter jederzeit Zugriff auf diese Daten.
    \item \textbf{Temperatur- und Luftfeuchtigkeitssensor (außen)}: Erfasst die klimatischen Bedingungen außerhalb des Containers.
    \item \textbf{Radarsensor}: Dient zur Erkennung von Bewegungen innerhalb des Containers, um die Anwesenheit von Personen festzustellen.
    \item \textbf{Türkontakt}: Überwacht den Öffnungszustand der Containertür, um Zutritte oder unbefugte Zugriffe zu erkennen.
\end{itemize}

In der Implementierung sind die Sensoren eindeutig über Prefixe und IDs abgebildet. Beispiele:

\begin{itemize}
    \item Türkontakt: \texttt{zb\_door\_1}
    \item Innenklima: \texttt{zb\_th\_inside\_1}
    \item Außenklima: \texttt{zb\_th\_outside\_1}
    \item Presence Detector: \texttt{zb\_appearance\_1}
\end{itemize}

Die Feldzuordnung zwischen MQTT-Payload und Datenbankschema wird in \texttt{config.json} über \texttt{properties} definiert. Beispiele: \texttt{state} $\rightarrow$ \texttt{status}, \texttt{contact} $\rightarrow$ \texttt{contact}, \texttt{temperature} $\rightarrow$ \texttt{temperature}. Dadurch kann ein Sensortyp ohne Codeänderung erweitert werden, solange Prefix und Mapping in der Konfiguration ergänzt werden.

Der \texttt{mqttListener} speichert nicht jede empfangene Nachricht. Stattdessen reduziert ein technischer Filter die Schreiblast:

\begin{itemize}
    \item Mindestintervall für reguläre Updates: 10 Minuten
    \item Sofortspeicherung bei Zustandswechseln (Tür/Presence)
    \item Schwellwerte: 2\,$^{\circ}$C (Temperatur), 5\,\% (Luftfeuchte), 1\,\% (Batterie), 5\,W (Leistung)
\end{itemize}

Dadurch bleibt die Datenhistorie fachlich aussagekräftig, ohne die Datenbank durch irrelevante Kleinständerungen zu überladen.

\textbf{Beispiel 1: Sensornachricht von Zigbee2MQTT}

\begin{lstlisting}[
caption={Beispiel fuer Topic und Payload eines Innenklima-Sensors},
label={lst:mqtt_sensor_example},
basicstyle=\ttfamily\small
]
Topic:
zigbee2mqtt/zb_th_inside_1

Payload:
{
    "temperature": 19.7,
    "humidity": 46.3,
    "battery": 91,
    "linkquality": 126
}
\end{lstlisting}

Im gezeigten Beispiel erkennt der \texttt{mqttListener} den Typ \texttt{th-inside} ueber den Prefix \texttt{zb\_th\_inside}. Anschliessend werden die Felder gemaess \texttt{config.json} auf das Datenbankschema gemappt und in \texttt{thInsideSensors} sowie \texttt{latestSensorData} geschrieben, sofern die Filterbedingungen erfuellt sind.

Für die Auswahl von Zigbee gegenüber WLAN und Bluetooth waren im Betrieb drei Kriterien entscheidend:

\begin{itemize}
    \item \textbf{Energieverbrauch}: Zigbee-Endgeräte sind für lange Batterielaufzeiten konzipiert, wodurch Wartungszyklen der Sensorik reduziert werden \cite{csa_zigbee_overview}.
    \item \textbf{Netzabdeckung im Containerumfeld}: Durch Mesh-Routing über Router-Knoten (z. B. netzversorgte Steckdosen) bleibt die Kommunikation auch bei baustellentypischen Abschattungen stabil \cite{digi_zigbee_mesh}.
    \item \textbf{Systemintegration}: Mit Zigbee2MQTT lassen sich Geräte vieler Hersteller in eine einheitliche MQTT-Schnittstelle überführen; dadurch sind die Topics in Home Assistant, Listener und Action-Handler konsistent nutzbar \cite{zigbee2mqtt_docs}.
\end{itemize}

\subsection{Aktorik und Energieverbraucher}

Im Container sind elektrische Verbraucher installiert, die über Zigbee-Steckdosen gesteuert werden:

\begin{itemize}
    \item \textbf{Heizung}
    \item \textbf{Lüftung}
    \item \textbf{Beleuchtung}
\end{itemize}

Die Steckdosen werden mithilfe des Zigbee-Protokolls gesteuert und ausgelesen. So kann beispielsweise die Heizung nur dann aktiviert werden, wenn der Container genutzt wird oder bestimmte Temperaturwerte unterschritten werden.

Die Aktoren sind in der Konfiguration als \texttt{plug-heating}, \texttt{plug-cooling} und \texttt{plug-light} mit \texttt{controllable=true} modelliert. Der Steuerpfad ist technisch klar definiert:

\begin{enumerate}
    \item Ein Steuerwunsch wird als Dokument in der Firestore-Collection \texttt{actions} abgelegt.
    \item Der \texttt{actionHandler} liest neue Dokumente per \texttt{onSnapshot}.
    \item Nach Validierung (\texttt{containerId}, \texttt{plugType}, \texttt{status}) wird das MQTT-Topic gebildet.
    \item Topic-Schema: \texttt{zigbee2mqtt/zb\_plug\_<plugType>\_<containerId>/set}
    \item Payload: \texttt{\{"state":"ON"\}} oder \texttt{\{"state":"OFF"\}}
    \item Bei erfolgreichem Publish wird das Action-Dokument gelöscht (Delete-on-success).
\end{enumerate}

Beispiel: Für \texttt{plugType=heating} und \texttt{containerId=1} wird auf \texttt{zigbee2mqtt/zb\_plug\_heating\_1/set} publiziert.

\textbf{Beispiel 2: Action-Dokument und resultierendes MQTT-Kommando}

\begin{lstlisting}[
caption={Beispiel fuer Firestore-Action und daraus erzeugtes MQTT-Steuerkommando},
label={lst:action_to_mqtt_example},
basicstyle=\ttfamily\small
]
Firestore-Dokument in actions:
{
    "containerId": 1,
    "plugType": "heating",
    "status": "ON",
    "timestamp": "2026-01-27T10:15:00Z"
}

Erzeugtes Topic:
zigbee2mqtt/zb_plug_heating_1/set

Erzeugte Payload:
{"state":"ON"}
\end{lstlisting}

Nach erfolgreichem Publish löscht der \texttt{actionHandler} das Dokument aus \texttt{actions}. Dadurch wird derselbe Schaltbefehl nicht mehrfach ausgefuehrt.

\subsection{Kamerasystem}

Für die sicherheitsrelevante Überwachung ist eine \textbf{4K-Kamera} installiert. Bild- oder Videodaten können zur späteren Auswertung, für Alarmmeldungen oder zur Fernüberwachung genutzt werden.

Die Kamera ist in der Architektur als eigenständige Sicherheitskomponente zu betrachten und nicht Teil des Zigbee-Datenpfads. Der aktuelle Projektstand nutzt Türkontakt und Presence Detector zur Erkennung sicherheitsrelevanter Ereignisse außerhalb der Betriebszeit. Eine automatische Alarmauslösung ist derzeit noch nicht implementiert; Ereignisse werden protokolliert und können im Dashboard ausgewertet werden.

\subsection{Kommunikation im Container}

Die Kommunikation folgt einem zweistufigen Modell:

\begin{itemize}
    \item \textbf{Feldebene}: Zigbee zwischen Sensor/Aktor und Dongle
    \item \textbf{Diensteebene}: MQTT zwischen Zigbee2MQTT, mqttListener, actionHandler und Home Assistant
\end{itemize}

Wesentliche technische Regel: \texttt{/set}-Topics sind reine Steuerkanäle und werden vom Listener nicht als Messdaten gespeichert. Dadurch bleiben Schreibpfad (Telemetry) und Steuerpfad (Commands) logisch getrennt.

\subsection{Cloud-Anbindung}

Mithilfe der \textbf{Firebase-Datenbank} werden die verarbeiteten Daten in typgetrennten Collections gespeichert, z. B. \texttt{doorSensors}, \texttt{thInsideSensors}, \texttt{thOutsideSensors}, \texttt{appearanceSensors} sowie die drei Plug-Collections. Zusätzlich wird je Gerät ein aktueller Zustand unter \texttt{latestSensorData} gehalten.

Für den Steuerpfad wird die Collection \texttt{actions} als Befehls-Queue verwendet. Das Backend oder Automatisierungslogik erzeugt dort Schaltbefehle, der Action-Handler führt sie aus und entfernt erfolgreich verarbeitete Einträge. Damit sind Zustandsspeicherung und Befehlsausführung im Datenmodell sauber getrennt.



\section{Systemarchitektur [Leopold Mistelberger]}
\setauthor{Elias Mahr}

\subsection{Kontextdiagramm}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pics/ContextDiagram.png}
    \caption{Kontextdiagramm des Hauptcontainers}
    \label{fig:kontextdiagramm}
\end{figure}

Abbildung~\ref{fig:kontextdiagramm} zeigt die Systemgrenze auf Kontextebene: Innerhalb des Containers werden Sensordaten erfasst und über lokale Dienste verarbeitet; außerhalb der Systemgrenze befinden sich externe Benutzer, Cloud-Dienste und übergeordnete Verwaltungsfunktionen. Das Kontextdiagramm dient hier als Einstieg, bevor die konkrete Hardware- und Softwarestruktur detailliert beschrieben wird.

\subsection{Hardware-Komponenten}

Die Hardwareebene umfasst alle physischen Komponenten zur Erfassung und Ausführung im Baustellencontainer:

\begin{enumerate}
    \item \textbf{Raspberry Pi als Edge-Gateway}: Der Raspberry Pi bündelt Sensor- und Aktordaten, hostet die lokalen Containerdienste und stellt die Schnittstelle zur Cloud dar.
    \item \textbf{Zigbee-Dongle}: Der USB-Dongle terminiert das Zigbee-Funknetz und reicht Messwerte sowie Steuerbefehle in die lokale MQTT-Welt weiter.
    \item \textbf{Sensoren}: Innen-/Außentemperatur, Luftfeuchte, Radarsensor und Türkontakt liefern Zustands- und Ereignisdaten.
    \item \textbf{Aktorik}: Schaltbare Verbraucher (Heizung, Lüftung, Beleuchtung) sind über Zigbee-Plugs eingebunden.
    \item \textbf{4K-Kamera}: Die Kamera liefert hochdetaillierte Bilddaten für Sicherheits- und Dokumentationszwecke.
\end{enumerate}

Die im Projekt konkret eingesetzten Komponenten sind:

\begin{itemize}
    \item \textbf{Sensoren}: Türkontakt (Zigbee), Temperatur-/Feuchtesensor innen (Zigbee), Temperatur-/Feuchtesensor außen (Zigbee), Presence Detector (Zigbee)
    \item \textbf{Zentrale}: Raspberry Pi 5 mit Gehäuse, Netzteil und USV-Modul (Pufferbatterie)
    \item \textbf{Funk- und Aktorkomponenten}: Zigbee USB-Dongle sowie Zigbee-Steckdosen zur Schaltung von Heizung, Lüftung und Beleuchtung
    \item \textbf{Netzwerk und Sicherheit}: Router sowie 4K-Kamera; optional sind Smart-Lock-Varianten (Bluetooth bzw. WLAN/Bluetooth/Matter) evaluierbar
\end{itemize}

Die Entscheidung für Zigbee ist hier architekturrelevant: Das Protokoll ist für Low-Power-IoT und Mesh-Topologien ausgelegt und reduziert dadurch den Wartungsaufwand bei batteriebetriebener Sensorik, während gleichzeitig Reichweite und Robustheit im Containerverbund verbessert werden \cite{csa_zigbee_overview}.

\subsection{Software-Komponenten}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pics/Systemarchitecture.png}
    \caption{Systemarchitektur ohne Deployment-Sicht}
    \label{fig:systemarchitektur}
\end{figure}

Abbildung~\ref{fig:systemarchitektur} zeigt die logische Softwarestruktur ohne Infrastruktur-Deployment. Die Komponenten lassen sich in drei Schichten gliedern:

\begin{enumerate}
    \item \textbf{Edge-Schicht (Raspberry Pi)}: Zigbee2MQTT, MQTT-Broker, Home Assistant, mqttListener.js und actionHandler.js.
    \item \textbf{Cloud- und Persistenzschicht}: Firebase für Telemetrie, Zustände, Regeln und Ereignisprotokolle.
    \item \textbf{Anwendungsschicht}: Angular-Frontend, C\#-Backend und Nginx als Reverse Proxy.
\end{enumerate}

\subsubsection{Datenfluss und Steuerpfad}

\begin{itemize}
    \item Sensordaten werden laufend erfasst und bei signifikanten Änderungen über Zigbee an den Raspberry Pi gesendet.
    \item Zigbee2MQTT transformiert proprietäre Zigbee-Frames in MQTT-Topics, sodass alle Geräte in einem einheitlichen Publish/Subscribe-Modell verfügbar sind \cite{zigbee2mqtt_usage}.
    \item mqttListener.js persistiert relevante Telemetrie in Firebase.
    \item actionHandler.js evaluiert Regeln und erzeugt Steuernachrichten für Aktoren.
    \item Home Assistant konsumiert dieselben Entitäten und kann zusätzlich Automatisierungen auf Ereignisbasis ausführen.
    \item Frontend und Backend greifen über REST auf konsolidierte Daten zu und ermöglichen manuelle Eingriffe.
\end{itemize}

\subsubsection{Eigene Sektion: mqttListener und actionHandler}

Die beiden Node.js-Dienste \texttt{mqttListener} und \texttt{actionHandler} bilden die zentrale Laufzeitlogik auf dem Raspberry Pi. Beide Dienste laufen als Container und sind auf unterschiedliche Aufgaben spezialisiert. Die Implementierung basiert auf dem npm-Paket \texttt{mqtt} für Broker-Kommunikation, \texttt{firebase-admin} für Firestore-Zugriff (im Listener) sowie einem gemeinsamen Konfigurationsmodell über \texttt{config.json}.

\textbf{Konfigurationsmodell (config.json)}


Die \texttt{config.json} ist das zentrale Bindeglied zwischen MQTT-Topics, internen Sensortypen und Firebase-Collections. Sie besteht aus vier funktionalen Blöcken:

\begin{itemize}
    \item \textbf{mqtt}: Broker-Host, Port und Credentials (im gezeigten Stand \texttt{mosquitto} auf Port \texttt{1883})
    \item \textbf{sensors}: Prefix-Definition und Feld-Mapping je Sensortyp
    \item \textbf{collections}: Zuordnung jedes Sensortyps auf eine konkrete Firestore-Collection
    \item \textbf{devices}: Geräteinventar mit Name, Typ und Containerzuordnung
\end{itemize}

Die Prefixe (z. B. \texttt{zb\_door}, \texttt{zb\_th\_inside}, \texttt{zb\_plug\_heating}) werden vom \texttt{mqttListener} zur Sensortyp-Erkennung verwendet. Das Feld-Mapping in \texttt{properties} übersetzt MQTT-Payload-Felder in das Datenbankschema, beispielsweise \texttt{state} $\rightarrow$ \texttt{status} bei Steckdosen.

Über den Block \texttt{collections} werden Messdaten typabhängig in getrennte Bereiche geschrieben (z. B. \texttt{doorSensors}, \texttt{thInsideSensors}, \texttt{plugHeatingSensors}). Dadurch bleibt die Datenstruktur fachlich sauber getrennt und Auswertungen können je Sensorklasse ohne zusätzliche Laufzeitfilter durchgeführt werden.

Der Block \texttt{devices} beschreibt zusätzlich die konkreten Instanzen (z. B. \texttt{zb\_door\_1}, \texttt{zb\_plug\_light\_1}) und markiert steuerbare Aktoren mit \texttt{controllable=true}. Damit ist die Konfiguration nicht nur technisch, sondern auch inventarbezogen dokumentiert.

\textbf{mqttListener (Ingestion und Dateneinsparung)}


Der \texttt{mqttListener} abonniert das Haupttopic \texttt{<mqtt.topic>/\#} und verarbeitet eingehende Zigbee2MQTT-Nachrichten. \texttt{/set}-Topics werden bewusst ignoriert, damit reine Steuerkommandos nicht als Sensormessungen gespeichert werden. Anschließend wird der Sensortyp über Prefix-Matching auf Basis der Konfiguration ermittelt.

Intern verwendet der Dienst mehrere In-Memory-Datenstrukturen:

\begin{itemize}
    \item \texttt{lastValues} als Cache des letzten bekannten Zustands je Sensor
    \item \texttt{lastUpdateTimes} zur zeitlichen Drosselung von Schreibvorgängen
    \item \texttt{lastStatusChangeTime} zur Entprellung häufiger ON/OFF-Wechsel bei Steckdosen
\end{itemize}

Für die Speicherung wird ein mehrstufiges Filterverfahren verwendet, um unnötige Datenbankeinträge zu vermeiden:

\begin{itemize}
    \item \textbf{Zeitfilter}: Standardmäßig wird nur in festen Zeitabständen gespeichert (aktuell 10 Minuten).
    \item \textbf{Ereignisfilter für Zustandswechsel}: Tür- und Presence-Sensoren werden bei echter Statusänderung sofort gespeichert.
    \item \textbf{Schwellwerte}: Temperatur, Luftfeuchte, Batteriestand und Steckdosenleistung werden nur bei signifikanten Änderungen geschrieben.
    \item \textbf{Entprellung bei Steckdosen}: Sehr schnelle ON/OFF-Wechsel werden über ein Mindestintervall gebremst.
\end{itemize}

Die Schwellwerte sind im Code explizit als technische Parameter hinterlegt (Temperatur 2\,$^{\circ}$C, Luftfeuchte 5\,\%, Batterie 1\,\%, Leistung 5\,W) und können zentral angepasst werden. Werden die Bedingungen erfüllt, schreibt der Dienst die Daten sowohl in die fachliche Ziel-Collection als auch in \texttt{latestSensorData} als aktuelle Sicht. Dadurch sind sowohl Historie als auch aktueller Zustand verfügbar.

Zusätzlich wird die \texttt{containerId} aus der Sensor-ID extrahiert und gemeinsam mit \texttt{sensorId}, gemappten Messfeldern und serverseitigem Zeitstempel gespeichert. Diese Normalisierung vereinfacht Auswertungen im Backend und reduziert Parser-Logik im Frontend.

\textbf{actionHandler (Steuerpfad für Aktoren)}


Der \texttt{actionHandler} ist der direkte Steuerdienst für die Steckdosen. Er liest neue Befehle aus der Firestore-Collection \texttt{actions} und übersetzt diese in MQTT-Kommandos für Zigbee2MQTT. Technisch erfolgt das über einen permanenten Snapshot-Listener (\texttt{onSnapshot}) auf \texttt{actions} mit Filter auf reale Dokumente (Dummy-Eintrag wird ausgeschlossen).

Die Kette \textbf{Firestore $\rightarrow$ MQTT $\rightarrow$ Steckdose} läuft in folgenden Schritten ab:

\begin{enumerate}
    \item In \texttt{actions} wird ein neues Dokument angelegt (\texttt{change.type = added}).
    \item Der \texttt{actionHandler} liest \texttt{actionId} und Nutzdaten aus dem Dokument.
    \item Die Nutzdaten werden validiert (Pflichtfelder und erlaubte Werte).
    \item Aus \texttt{plugType} und \texttt{containerId} wird das Zieltopic berechnet.
    \item Das Kommando wird als JSON-Payload via MQTT publiziert.
    \item Bei erfolgreichem Publish wird das Action-Dokument gelöscht, damit derselbe Befehl nicht erneut ausgeführt wird.
\end{enumerate}

Der Ablauf ist dabei strikt validiert:

\begin{itemize}
    \item Pflichtfelder: \texttt{containerId}, \texttt{plugType}, \texttt{status}
    \item Erlaubte Statuswerte: \texttt{ON} oder \texttt{OFF}
    \item Erlaubte Plug-Typen: \texttt{light}, \texttt{heating}, \texttt{cooling}
\end{itemize}

Das MQTT-Topic wird dynamisch nach dem Schema \texttt{zigbee2mqtt/zb\_plug\_<plugType>\_<containerId>/set} aufgebaut. Als Payload wird \texttt{\{"state":"ON|OFF"\}} gesendet. Beispiel: \texttt{plugType=heating}, \texttt{containerId=1} ergibt \texttt{zigbee2mqtt/zb\_plug\_heating\_1/set}.

Wichtig für die Betriebssicherheit: Ungültige Actions (fehlende Felder, falscher Status oder unbekannter Plug-Typ) werden verworfen und aus \texttt{actions} entfernt. Bei MQTT-Publish-Fehlern wird die Action hingegen nicht als erfolgreich behandelt; damit bleibt der Fehlerzustand nachvollziehbar und kann erneut verarbeitet oder manuell geprüft werden.

Die Service-Logik folgt damit dem Muster \textit{command queue in Firestore} $\rightarrow$ \textit{validiertes MQTT-Command} $\rightarrow$ \textit{Delete-on-success}. Dieses Muster ist für die gewählte Architektur zweckmäßig, da die auszuführenden Steuerbefehle explizit nachvollziehbar sind und nach erfolgreicher Ausführung nicht in der Queue verbleiben.

\textbf{Robustheit und Laufzeitverhalten}


Beide Dienste behandeln Laufzeitfehler aktiv. Der \texttt{actionHandler} besitzt eine Reconnect-Logik mit zeitverzögertem Neuverbindungsversuch (5 Sekunden) bei MQTT-Fehlern oder Verbindungsabbruch. Der \texttt{mqttListener} behandelt Parser- und MQTT-Fehler über zentrale Event-Handler und setzt die Verarbeitung für nachfolgende Nachrichten fort.

Zusätzlich unterstützen beide Services einen Graceful Shutdown über \texttt{SIGINT}/\texttt{SIGTERM}, wodurch offene MQTT-Verbindungen sauber geschlossen werden. Dadurch sinkt das Risiko von inkonsistenten Verbindungszuständen bei Container-Neustarts oder Deployments.

\subsubsection{Automatisierungen direkt in Home Assistant}

Home Assistant wird nicht nur zur Visualisierung eingesetzt, sondern als lokale Automatisierungs-Engine. Automationen bestehen aus Trigger, Bedingungen und Aktionen \cite{ha_automation_basics}. Für das CESC-Szenario sind folgende Automatisierungen direkt in Home Assistant umsetzbar:

\begin{itemize}
    \item \textbf{Temperaturregelung}: Bei Unterschreitung eines Sollwerts wird die Heizung automatisch aktiviert, bei Überschreitung deaktiviert.
    \item \textbf{Präsenzabhängige Lüftung}: Lüftung wird nur bei erkannter Anwesenheit und zu hoher Luftfeuchtigkeit aktiviert.
    \item \textbf{Einbruchserkennung außerhalb Betriebszeit}: Bei Kombination aus Türkontakt und Bewegung außerhalb der Betriebszeit wird derzeit ein sicherheitsrelevantes Ereignis protokolliert; eine aktive Alarmierung (z. B. Push/Sirene) ist im aktuellen Stand noch nicht implementiert.
    \item \textbf{Licht-Automation}: Bei Präsenz und zu geringer Umgebungshelligkeit wird Beleuchtung eingeschaltet, sonst ausgeschaltet.
\end{itemize}

Ein konkretes Beispiel für eine Home-Assistant-Automation (Temperaturregelung) ist in Listing~\ref{lst:ha_automation_temp} dargestellt:

\begin{lstlisting}[
caption={Home Assistant Automation: Heizung bei niedriger Innentemperatur},
label={lst:ha_automation_temp},
basicstyle=\ttfamily\small
]
alias: Heizung bei Kaltstart aktivieren
trigger:
  - platform: numeric_state
        entity_id: sensor.zb_th_inside_1_temperature
    below: 18
condition:
  - condition: state
        entity_id: binary_sensor.zb_appearance_1_presence
    state: "on"
action:
  - service: switch.turn_on
    target:
            entity_id: switch.zb_plug_heating_1
mode: single
\end{lstlisting}

Die Trigger- und Condition-Modelle entsprechen der offiziellen Home-Assistant-Spezifikation und sind dadurch direkt wartbar und versionierbar \cite{ha_automation_trigger}.




\section{Deployment}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pics/SystemarchitectureWithDeployment.png}
    \caption{Systemarchitektur mit Deployment-Sicht}
    \label{fig:systemarchitektur_deployment}
\end{figure}

Abbildung~\ref{fig:systemarchitektur_deployment} ergänzt die logische Sicht aus Abbildung~\ref{fig:systemarchitektur} um die physische Verteilung der Komponenten. Sichtbar ist, welche Container auf dem Raspberry Pi und welche Dienste auf dem Server laufen sowie aus welcher Registry die Images bezogen werden.

\subsection{Build-Prozess}

Der Build-Prozess beschreibt die automatisierte Erstellung von lauffähigen Softwarekomponenten in Form von Docker-Images. Ziel ist es, den Quellcode des Systems in standardisierte, ausführbare Einheiten zu überführen, die unabhängig von der Zielplattform eingesetzt werden können.

Im Rahmen des CESC-Projekts wird dieser Prozess vollständig automatisiert durchgeführt. Jede Änderung am Quellcode führt dazu, dass die betroffenen Komponenten neu gebaut und bereitgestellt werden. Dadurch wird sichergestellt, dass alle Systeme stets den aktuellen Entwicklungsstand verwenden.

\subsubsection{Continuous Integration mit GitHub Actions}

Zur Automatisierung des Build-Prozesses wird eine Continuous-Integration-Pipeline eingesetzt. Diese wird über GitHub Actions realisiert und bei jeder Änderung im Hauptbranch (\texttt{main}) ausgelöst \cite{github_actions_overview}.

Im Projekt sind drei konkrete Workflow-Dateien definiert:

\begin{itemize}
        \item \textbf{Action Handler Workflow}: baut und veröffentlicht das Image \texttt{action-handler:latest}
        \item \textbf{Frontend/Backend Workflow}: baut und veröffentlicht \texttt{cesc-app-backend:latest} und \texttt{cesc-app-frontend:latest}
        \item \textbf{MQTT Listener Workflow}: baut und veröffentlicht \texttt{mqtt-listener:latest}
\end{itemize}

Die Trigger sind jeweils pfadbasiert, damit nur bei relevanten Änderungen ein Build startet.

\textbf{Trigger: Action Handler}


\begin{verbatim}
on:
  push:
    branches: [ main ]
    paths:
            - 'raspi/actionHandler.js'
            - 'raspi/firebase.js'
            - 'raspi/Dockerfile.actionHandler'
            - '.github/workflows/action-handler.yaml'
\end{verbatim}

\textbf{Trigger: Frontend und Backend}


\begin{verbatim}
on:
    push:
        branches: [ main ]
        paths:
            - 'cesc-app/**'
\end{verbatim}

\textbf{Trigger: MQTT Listener}


\begin{verbatim}
on:
    push:
        branches: [ main ]
        paths:
            - 'raspi/**'
            - '.github/workflows/mqtt-listener.yaml'
\end{verbatim}

Durch die Einschränkung auf bestimmte Pfade wird sichergestellt, dass nur relevante Änderungen einen Build-Prozess auslösen. Dies reduziert unnötige Builds und verbessert die Effizienz der Pipeline.

\subsubsection{Ablauf des Build-Prozesses}

Der Build-Prozess besteht aus mehreren aufeinanderfolgenden Schritten:

\begin{enumerate}
    \item \textbf{Code Checkout}: Der aktuelle Stand des Repositories wird geladen.
    
    \item \textbf{Authentifizierung}: Anmeldung bei der Container Registry, um Images speichern zu können.
    
        \item \textbf{Konfigurationsgenerierung}: Sensible Konfigurationsdateien werden zur Laufzeit aus sicheren Variablen (Secrets) erzeugt. Verwendet werden dabei \texttt{RASPI\_CONFIG\_JSON}, \texttt{FIREBASE\_CREDENTIALS\_JSON} und \texttt{ENVIRONMENT}.
    
    \item \textbf{Image-Build}: Erstellung eines Docker-Images basierend auf einem Dockerfile.
    
    \item \textbf{Image-Push}: Hochladen des fertigen Images in die Container Registry.
\end{enumerate}

Für den Action Handler und den MQTT Listener wird zusätzlich Docker Buildx verwendet, um Multi-Arch-Images für \texttt{linux/amd64} und \texttt{linux/arm64} zu veröffentlichen. Dadurch können dieselben Images sowohl auf klassischen Servern als auch auf ARM-basierten Zielsystemen eingesetzt werden.

Der Frontend/Backend-Workflow arbeitet mit zwei getrennten Builds. Vor dem Backend-Build wird \texttt{firebase-credentials.json} aus einem Secret geschrieben und nach dem Push wieder gelöscht. Für das Frontend werden \texttt{environment.interface.ts}, \texttt{environment.ts} und \texttt{environment.prod.ts} temporär erzeugt, im Build verwendet und anschließend entfernt.

\subsubsection{Abgrenzung: Build/Push statt Test-Pipeline}

Die aktuellen Workflows sind bewusst als Build- und Deployment-Workflows ausgelegt. Automatisierte Test-Stufen (z. B. Unit-Tests, Integrationstests, Linting als Quality Gate) sind im aktuellen Stand noch nicht integriert. Der Fokus liegt derzeit auf reproduzierbarem Container-Build und schneller Bereitstellung neuer Versionen.

\subsection{Rollout auf Zielsysteme}

Nach dem Push in die Registry werden die Container auf den Zielsystemen aktualisiert. Der technische Ablauf ist deterministisch und kann auf Raspberry Pi und Server identisch ausgeführt werden:

\begin{enumerate}
    \item \textbf{Image-Pull}: Abruf der neuen Version aus \texttt{ghcr.io}.
    \item \textbf{Container-Neustart}: Stoppen des alten Containers, Start der neuen Version.
    \item \textbf{Health-Prüfung}: Prüfung, ob HTTP-Endpunkte bzw. MQTT-Verbindungen verfügbar sind.
    \item \textbf{Rollback bei Fehler}: Falls Health-Checks fehlschlagen, Rückfall auf das letzte stabile Image-Tag.
\end{enumerate}

Dieses Vorgehen reduziert Downtime und macht den Deploymentstatus reproduzierbar prüfbar. Für Frontend und Backend erfolgt das Deployment über die neu gebauten Images aus \texttt{ghcr.io}; für die Raspberry-Pi-Dienste analog über die Images von Action Handler und MQTT Listener.

\subsubsection{Verwendung von Secrets}

Sensible Daten wie Zugangsdaten oder API-Schlüssel werden nicht im Quellcode gespeichert, sondern als sogenannte Secrets in der CI/CD-Umgebung hinterlegt.

Während des Build-Prozesses werden diese Daten temporär in Konfigurationsdateien geschrieben. 

\begin{verbatim}
echo '${{ secrets.RASPI_CONFIG_JSON }}' > ./raspi/config.json
\end{verbatim}
\label{lst:secret_example}

Um sicherzustellen, dass die generierten Konfigurationsdateien gültig sind, erfolgt eine Validierung, beispielsweise mittels des Tools \texttt{jq}:

\begin{verbatim}
jq . ./raspi/config.json > /dev/null
\end{verbatim}

Nach Abschluss des Build-Prozesses werden diese Dateien wieder entfernt, um ein Sicherheitsrisiko zu vermeiden.

\subsubsection{Container Registry}

Die erstellten Docker-Images werden in einer zentralen Container Registry gespeichert. Diese dient als Verteilplattform für alle Systemkomponenten.

Im vorliegenden System wird die GitHub Container Registry verwendet. Die Images werden mit einem eindeutigen Namen und Tag versehen, beispielsweise:

\begin{verbatim}
ghcr.io/<repository>/action-handler:latest
\end{verbatim}

Andere Systeme, wie der Raspberry Pi oder der Server, können diese Images anschließend direkt herunterladen und ausführen.

\subsubsection{Vorteile des automatisierten Build-Prozesses}

Der automatisierte Build-Prozess bietet mehrere Vorteile:

\begin{itemize}
    \item Konsistente und reproduzierbare Builds
    \item Reduzierung manueller Fehler
    \item Schnelle Bereitstellung neuer Versionen
    \item Zentrale Verwaltung aller Softwarekomponenten
    \item Multi-Arch-Bereitstellung für unterschiedliche Zielplattformen
\end{itemize}


\section{Server- und Infrastrukturtechnologien [Elias Mahr]}
\setauthor{Elias Mahr}


\section{Keycloak [Elias Mahr]}
\setauthor{Elias Mahr}

\subsection{Einbindung ins Frontend}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.5]{pics/FrontendButton.png}
    \caption{Button im Frontend}
    \label{fig:FrontendButton}
\end{figure}

Um die Webapp möglichst unkompliziert und benutzerfreundlich zu halten, ist der Button, wie im Screenshot ersichtlich, rechts oben in der Ecke platziert. Unabhängig vom Scroll-Zustand oder der aktuell ausgewählten Page bleibt er auch immer an dieser Stelle, um ein schnelles, unkompliziertes Anmelden zu gewährleisten.

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.5\textwidth]{pics/anmelden.png}
        \caption{Anmelde-Button}
        \label{fig:anmeldebutton}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.5\textwidth]{pics/abmelden.png}
        \caption{Abmelde-Button}
        \label{fig:abmeldebutton}
    \end{subfigure}
    \caption{Login- und Logout-Buttons}
    \label{fig:loginbuttons}
\end{figure}

Das Icon stammt aus der Font Awesome Bibliothek:
\\
\lstinline[language=HTML]{<i class="fas fa-sign-in-alt"></i>}
\\
Bei Klick darauf wird man weitergeleitet zum Pfad:\\ \texttt{https://192.168.20.202/auth/realms/cesc/protocol/openid-connect\\/auth?client\_id=cesc-frontend\&redirect\_uri=https...} 
Unter dieser Url wird der Standard-Login-Dialog von Keycloak geöffnet. Nach erfolgreicher Anmeldung bekommt der User seinen Token, wird automatisch wieder zur vorher besuchten Seite umgeleitet und es werden alle Funktionen der Webseite freigeschaltet.

\subsection{Der Realm}
Ein Realm in Keycloak ist ein isolierter Sicherheitsbereich. Er umfasst Benutzer, Rollen, Clients und Policies für eine bestimmte Anwendung. Der Realm namens ``cesc'' definiert die gesamte Identifikation und Authorisierung für die Weboberfläche. Zurzeit ist dieser Bereich möglichst klein gehalten, jedoch vollkommen ausreichend. Ein Admin User(herbsthofer) steuert alles, in Zukunft kann man jedoch leicht noch andere Benutzer/Rollen hinzu fügen.
\subsubsection{Realm Einstellungen}

\begin{lstlisting}[
    caption=Keycloak Realm Konfiguration,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]
{
  "realm": "cesc",
  "enabled": true,
  "sslRequired": "external",
  "registrationAllowed": false,
  "loginWithEmailAllowed": true,
  "duplicateEmailsAllowed": false,
  "resetPasswordAllowed": true,
  "editUsernameAllowed": false,
  "bruteForceProtected": true
}
\end{lstlisting}
In den wichtigsten Einstellungen wird unter anderem definiert, dass für alle externen Verbindungen SSL vorgeschrieben ist.
Die Zeile \texttt{bruteForceProtected: true} ist eine einfache Absicherung gegen Brute Force Angriffe auf Keycloak. Durch sie wird ein Account nach mehreren fehlgeschlagenen Anmeldungen temporär gesperrt.
Um zukünftigen Admins der Webseite eine leichte und unkomplizierte Anmeldung zu ermöglichen, ist auch das Einloggen mit der E-Mail-Adresse erlaubt.

\subsubsection{Rollen}
\begin{lstlisting}[
    caption=Keycloak Realm Konfiguration,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]
{
  "roles": {
    "realm": [
      {
        "name": "admin",
        "description": "Administrator role for CESC application",
        "composite": false,
        "clientRole": false
      }
    ]
  }
}
\end{lstlisting}
Der aktuell einzige Benutzer \texttt{``admin''} hat sozusagen uneingeschränkte Rechte. Durch die Zeile \texttt{" clientRole " : false} ist dieser Benutzer auch nicht auf einen bestimmten Client beschränkt, sondern hat im ganzen Realm auf alles Zugriff.

\subsubsection{Benutzer}
\begin{lstlisting}[
    caption=Keycloak Realm Konfiguration,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]
{
"users": [
    {
      "username": "herbsthofer",
      "enabled": true,
      "emailVerified": true,
      "firstName": "Admin",
      "lastName": "Herbsthofer",
      "email": "herbsthofer@example.com",

      "realmRoles": [
        "admin"
      ]
    }
  ]
}
\end{lstlisting}
Der Zurzeit einzige User \texttt{herbsthofer} erhält durch die Zuweisung der admin Rolle, \texttt{'' realmRoles '' : ['' admin '']} alle Administratorrechte.
\subsubsection{Clients}
\begin{lstlisting}[
    caption=Keycloak Realm Client Konfiguration,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]
{
   "clients": [
    {
      "clientId": "cesc-frontend",
      "name": "CESC Frontend Application",
      "enabled": true,
      "publicClient": true,
      "protocol": "openid-connect",
      "standardFlowEnabled": true,
      "redirectUris": [
        "https://192.168.20.202/*",
        "http://localhost:4200/*"
      ],
      "webOrigins": [
        "https://192.168.20.202",
        "http://localhost:4200"
      ]
    },
    {
      "clientId": "cesc-backend",
      "name": "CESC Backend API",
      "enabled": true,
      "publicClient": false,
      "bearerOnly": true,
      "protocol": "openid-connect"
    }
  ]
}
\end{lstlisting}
Für unsere Anwendung werden nur zwei Clients benötigt. Der \texttt{cesc-frontend} Client ist der öffentliche Client (\texttt{publicClient: true}) für die Angular Anwendung. Die \texttt{redirectUris} und \texttt{webOrigins} legen die erlaubten URLs für die Produktion (192.168.20.202) und die lokale Entwicklung (localhost:4200) fest. Der \texttt{cesc-backend} Client ist als vertraulicher Client gemacht, da er keine Login Page bereitstellt.(\texttt{publicClient: false}).

\section{Frontend: Angular}
\setauthor{Elias Mahr}

Für die Implementierung des Frontends wurde Angular als Framework gewählt. Ausschlaggebend war insbesondere die klare, komponentenbasierte Architektur. Da das Projekt mehrere fachlich unterschiedliche Bereiche umfasst, wie Dashboard, Kalenderlogik, Authentifizierung, Steuerfunktionen und Protokolle, war eine strukturierende Technologie mit klaren Unterteilungen besonders wichtig.

Angular bietet hierfür eine gut Voraussetzung mit integrierten Mechanismen für Routing, Dependency Injection und reaktive Datenverarbeitung. Dadurch konnten wiederkehrende Aufgaben einheitlich umgesetzt und Schnittstellen zwischen Komponenten, Services und Backend sauber definiert werden. Im Vergleich zu leichter gewichtigen Ansätzen ist Angular in der Entwicklung aufwändiger, jedoch hinsichtlich auf die Größe des Projektes betrachtet notwendig. Die Firma wollte eine Software auf Unternehmensstandart und für diese ist Angular die richtige Wahl.

\subsection{Designsysthem}
Im Rahmen des UI-Designsystems wurde eine konsistente Farbpalette global im Projekt definiert, um Funktion, Zustand und Priorität von Angezeigtem eindeutig zu kommunizieren. Ziel war nicht nur eine einheitliche visuelle Gestaltung, sondern vor allem eine schnelle Erfassbarkeit relevanter Zustände im laufenden Betrieb. Die Farbauswahl orientiert sich daher am Ampelsystem: positives (z. B. aktiv, erfolgreich) werden in grünen Tönen dargestellt, Warn- und Zwischenzustände in gelb/orange und kritische Zustände (z. B. Fehler, deaktiviert, nicht verfügbar) in rot bzw. grauen Tönen.

Dieses Farbschema wird komponentenübergreifend angewendet, etwa bei Türstatus, Plugzuständen sowie Fehlermeldungen. Dadurch entsteht ein konsistentes Erscheinungsbild mit hohem Wiedererkennungswert. 

\definecolor{tmp1}{HTML}{4CAF50}
\definecolor{tmp2}{HTML}{FF6B35}
\definecolor{tmp3}{HTML}{dc3545}

\begin{itemize}
\item \textcolor{tmp1}{Success}
\item \textcolor{tmp2}{Warning}
\item \textcolor{tmp3}{Error}
\end{itemize}

Neben den Zustandsfarben wurden auch Grundfarben für Hintergründe und Container festgelegt, um Lesbarkeit und Kontrast sicherzustellen. Diese Farben Orientieren sich an den Grundfarben der Firma. Das Ergebnis ist ein Designsystem, das sowohl ästhetisch als auch funktional begründet ist und damit die user experience im Dashboard nachhaltig verbessert.

\definecolor{HerbsthoferYellow}{HTML}{F0E620}
\definecolor{HerbsthoferBlack}{HTML}{000000}
\definecolor{HerbsthoferLightGray}{HTML}{E8E8E8}

\begin{itemize}
\item \textcolor{HerbsthoferBlack}{Herbsthofer Schwarz}
\item \textcolor{HerbsthoferLightGray}{Herbsthofer Grau}
\item \textcolor{HerbsthoferYellow}{Herbsthofer Gelb}
\end{itemize}

\subsection{Signals}
Angular Signals wurden in unserem Frontend zur lokalen UI-Zustandsverwaltung eingesetzt. Ein Signal ist dabei ein typsicherer Zustand, der bei Änderung automatisch die betroffenen Bereiche aktualisiert. Im Unterschied zu klassischen, manuell verwalteten Variablen reduziert dieses Konzept den Synchronisationsaufwand zwischen Logik und UI und verbessert die Nachvollziehbarkeit von Zustandsänderungen.

Im Projekt werden Signals insbesondere in der Dashboard-Komponente verwendet, unter anderem um Zustände der laufenden Steckdosen-Schaltvorgänge je Plug-Typ zu verwaltet, wodurch Buttons und Statusanzeigen direkt auf Aktionen reagieren können. Des Weiteren werden in der Dashboard-Komponente zum Anzeigen der vielen anderen Sensor Daten immer Signals verwendet. Zusätzlich werden Signals in der Startlisten-Komponente genutzt, um die aktuell gewählte Baustelle und die verfügbaren Baustellenlisten zu verwalten.

Insgesamt tragen Signals im Projekt dazu bei, UI-Reaktionen direkter und klarer insbesondere in Bereichen mit häufigen Zustandswechseln durch Live-Daten von Sensoren.

\subsection{Components}

Komponenten sind in Angular die Grundlage der Webseite. Sie dienen zur einfachen und übersichtlichen Strukturierung der Anwendung. Durch klare Code Trennungen ist alles übersichtlich und bei späteren Verbesserungen/Änderungen leichter zu Warten.
Eine Anwendung wird nicht in einem einzigen html File erstellt sondern klar in mehrere kleinere Elemente, bestehend aus eigenen html, css, und typescript Files, unterteilt. Diese kombiniert ergeben dann erst das große ganze, somit wird auf der einen Seite die Architektur so Sauber wie möglich gehalten und auf der anderen Seite die Wartung so einfach wie möglich vorbereitet.
Um so eine Komponente zu erstellen kann man ganz einfach die Angular CLI benutzen. Mit dem Befehl
\begin{lstlisting}[language=bash, caption={erstellen einer Komponente in der Angular CLI}]
ng generate component NAME
\end{lstlisting}
werden alle benötigten Files erstellt.

\subsubsection{Komponenten Baum}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.12]{pics/ComponentTree.png}
    \caption{Component Tree}
    \label{fig:ComponentTree}
\end{figure}

\subsubsection{start-list-container}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.2]{pics/start-list-container.png}
    \caption{Startlist Component}
    \label{fig:FrontendButton}
\end{figure}

Die ``StartListContainerComponent'' dient als Startseite der Anwendung.
Beim Öffnen wird eine komplette Übersicht über alle in der Datenbank gespeicherten Baustellen angezeigt.
Die Seite ist in drei wesentliche Abschnitte unterteilt: 
\begin{itemize}
  \item \textbf{Titel}
  
  Ganz ober sieht man sofort den Titel unserer Arbeit(C-E-S-C) mit dem ausgeschriebenen Titel darunter(Container-Energy-Security-Controll).
  \item \textbf{Baustellenübersicht}
  
  Links auf 1/4 der Breite sind alle Baustellen mit ihrem in der Datenbank gespeicherten Namen aufgelistet. Die ausgewählte Baustelle wird immer gelb mit Rahmen markiert, so auch beim Überfahren mit der Maus. In diesem Fall wird das Element auch ein wenig eingerückt. Immer am Ende des Abteils ist das Kalenderelement. Dieses ist blau eingefärbt, damit man es gut von den Baustellen unterscheiden kann.
  \item \textbf{Containerübersicht}
  
  Rechts bis mittig zu 3/4 der Breite ist die Übersicht der jeweiligen Container zu einer Baustelle. Mit dem Wechsel der Baustelle wird auch die Ansicht der Container aktualisiert. Bei dieser Ansicht werden Hauptcontainer optisch hervorgehoben.
  
\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.8\textwidth]{pics/mainContainer.png}
        \caption{Main Container}
        \label{fig:anmeldebutton}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.8\textwidth]{pics/normalContainer.png}
        \caption{normaler Container}
        \label{fig:abmeldebutton}
    \end{subfigure}
    \caption{Unterschiedliche Darstellung der Container}
    \label{fig:loginbuttons}
\end{figure}

  Hauptcontainer erkennt man am gelben Rand und der Kennzeichnung ``MAIN'' neben dem Containernamen.
  Mit Klick auf eine Componente wird man zum jeweiligen Dashboard weitergeleitet.
  Der Name(im Screenshot z.B. \texttt{Container\_1}), wird aus der Datenbank
  genommen und kann natürlich auf Wunsch geändert werden.
  Deffaultmäßig ist es zurzeit ``\texttt{Container\_n}''.

\end{itemize}



\subsubsection{Calendar}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.15]{pics/CalenderEditor.png}
    \caption{Kalenderelement}
    \label{fig:CalenderEditor}
\end{figure}
Die \texttt{Calender} Componente erscheint, wenn man in der \text{start-list-component} keine Baustelle, sondern das blaue \text{Kalenderelement} 
 antippt. Als erstes sieht man sofort den Kalender, bei dem immer das aktuelle Monat ausgewählt ist.
Im Kalender wird zwischen zwei Arten von Urlauben unterschieden.

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.3\textwidth]{pics/Betriebsurlaub.png}
        \caption{Betriebsurlaub}
        \label{fig:betriebsurlaub}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.29\textwidth]{pics/Feiertag.png}
        \caption{Feiertag}
        \label{fig:feiertag}
    \end{subfigure}
    \caption{Unterschied der Urlaube}
    \label{fig:holidays}
\end{figure}

Betriebsurlaub wird gelb markiert und ein Feiertag grau.
An diesen markierten Tagen werden die Container nicht eingeschaltet.
Unter dem Kalender findet man noch eine Liste mit allen Urlauben für das aktuelle Jahr und den Betriebsurlauben.
Die Feiertage werden von einer API geladen. Nach erfolgreicher Anmeldung wird der ``bearbeiten'' Button freigeschaltet und Umänderungen können vorgenommen werden.

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.4\textwidth]{pics/Freigschalten.png}
        \caption{Freigeschalten}
        \label{fig:betriebsurlaub}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.42\textwidth]{pics/lockd.png}
        \caption{ohne Anmeldung}
        \label{fig:feiertag}
    \end{subfigure}
    \caption{Bearbeiten Button}
    \label{fig:bearbeiten}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.15]{pics/calender-component.png}
    \caption{Betriebsurlaube bearbeiten}
    \label{fig:Calender}
\end{figure}

Bei Klick auf den Butten kann der Betriebsurlaub bearbeitet werden.
Die neuen Daten werden direkt in die Datenbank gespeichert und sofort vom Cronjob beachtet. Dabei wird natürlich auch geprüft, ob die Daten im korrekten Format eingeben wurden.
Um dies zu erleichtern wird beim Eingeben eines neuen Datums sofort ein kleiner Kalender geöffnet, auf dem ein neues Datum ausgewählt werden kann. Dies ist eine vorgefertigte Funktion von html wenn man die input type date verwendet.

\begin{lstlisting}[
    caption=label im html,
    label=lst:htmlcalender,
    basicstyle=\ttfamily\small
]
{
    <label>Von: <input type="date" [(ngModel)]="modalFrom"></label>
}
\end{lstlisting}


\subsubsection{dashboard}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.18]{pics/dashboard.png}
    \caption{Dashboard}
    \label{fig:Dashboard}
\end{figure}

Wenn man auf den Button \texttt{Dashboard} in der \texttt{start-list-componente} bei einem Container klickt, kommt man zur \texttt{dashboard} Komponente. 
Diese Hauptfunktion der Applikation ist für jeden Container individuell abhängig, von den jeweiligen Daten.
Der Aufbau ist jedoch überall gleich. Die Componente ist so gestaltet, dass sie jederzeit erweitert werden kann. 
Für jeden Sensor ist ein eigener abgetrennter Abschnitt auf der Webseite. Bei Erweiterung des Systems durch die Firma
kann ein weiterer Abschnitt jederzeit leicht hinzugefügt werden, ohne Rücksicht auf die anderen Componenten nehmen zu müssen.

Ganz oben in der Componente sieht man die Überschrift mit dem Namen des Containers. Der erste Abschnitt ist eine übersichtliche Temperaturanzeige von Innen- und Außensensor.
Fettgedruckt ist die Temperatur, da das die wichtigste Anzeige im Container ist. Gleich darunter steht etwas kleiner die Luftfeuchtigkeit und abschließend noch die Anzeige der übrigen Batterie, um rechtzeitig zu erkennen, wann die Batterien gewechselt werden sollten.
Ein detaillierter Verlauf kann dann noch mit dem Button ``Verlauf'' angezeigt werden. Dazu aber dann in dieser Componente mehr.

Darunter befinden sich der Anwesenheitsstatus und der Tür Status. Diese sind durch Icons sofort ersichtlich. 

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.3\textwidth]{pics/AnwesenheitTrue.png}
        \caption{Anwesenheit True}
        \label{fig:anwesenheitTrue}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.29\textwidth]{pics/AnwesenheitFalse.png}
        \caption{Anwesenheit False}
        \label{fig:anwesenheitFalse}
    \end{subfigure}
    \caption{Icons Anwesenheit}
    \label{fig:anwesenheitIcons}
\end{figure}

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.3\textwidth]{pics/DoorTrue.png}
        \caption{Tür zu}
        \label{fig:doorOpen}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=0.29\textwidth]{pics/DoorFalse.png}
        \caption{Tür offen}
        \label{fig:door Close}
    \end{subfigure}
    \caption{Unterschied der Urlaube}
    \label{fig:door Icons}
\end{figure}


In der Mitte kommt man zum Button "Tür logs anzeigen". Dieser navigiert zur door-logs Komponente mit den jeweiligen Daten zum Cointainer.

Im Elemente Steuerung gibt es 3 Unterteilungen in "Beleuchtung", "Heizung" und "Kühlung". Durch einen klick auf den Toggle kann dann im Container der jeweilige Plug ein/aus geschaltet werden.

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.27]{pics/Steuerung.png}
    \caption{Steuerung}
    \label{fig:steuerung}
\end{figure}

Das Ende der Komponente ist der "Kalender bearbeiten" Button. Dieser ist bei fehlender Autorisierung disabled. Nach erfolgreicher Anmeldung kann hier zur calendar-component navigiert werden.

\subsubsection{door-logs}

\begin{figure}[H]
    \centering
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=1\textwidth]{pics/doorLogs.png}
        \label{fig:doorLogsUp}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.45\textwidth}
        \centering
        \includegraphics[width=1\textwidth]{pics/doorLogsDown.png}
        \label{fig:doorLogsdown}
    \end{subfigure}
    \caption{Door Logs Componente}
    \label{fig:doorLogs}
\end{figure}

Ein Wesentlicher Bestandteil unserer Arbeit ist es die Sicherheit gegen Einbrüche in die Container zu verbessern.
Da unsere erste Überlegung mit der Kamera, Datenschutztechnisch sehr schwierig umzusetzen ist und sich in einer Rechtlichen Grauzone befindet wurde eine andere Lösung gesucht.
Nach Gesprächen mit der Firma wurde das Konzept der door-logs Komponente entwickelt.
Die Komponente zeigt die Historie aller Türöffnungen des Containers auf.
Um unerlaubtes eindringen außerhalb der Arbeitszeit nachvollziehen zu können sind in dieser Komponente im nachhinein alle Öffnungen aufgezeigt.


Aufgebaut ist sie in drei Abschnitte.
Einmal die Überschrift Tür-Logs ganz oben auf der Seite.
Als zweites ein Menüband um mögliches filtern zu ermöglichen. Es kann eine Zeitraum Filterung und eine Filterung auf nur jene Öffnungen außerhalb der Arbeitszeit angewendet werden. Bei der Zeitraum Filterung sind standartmäßig die letzten sieben Tage eingestellt.
In einem Dropdown werden die Möglichkeiten "letzter Monat" und "alle Daten" noch aufgezeigt. Das Dropdown kann jedoch jederzeit auf andere Zeiträume erweitert werden da der genutzte Endpunkt beliebig viele Tage zurückgeben kann.
Sollte die Checkbox "Nur außerhalb der Arbeitszeiten" werden nur Türöffnungen außerhalb der Arbeitszeiten angezeigt, der Filter des Zeitraums wird hierbei auch berücksichtigt.

Der Letzte Abschnitt ist die Liste. Zum schnellen Erkennung von unrechtmäßigen eindringen außerhalb der Arbeitszeit werden solche Öffnungen Rot hervorgehoben. Angezeigt wird für jeden Öffnung das Datum, die Zeit wann die Türe geöffnet wurde und zu welcher Zeit sie wieder geschlossen wurde.
Am Fuße der Liste finden sich noch zwei Buttons. der gelbe Button "Zurück" ist lediglich dazu da wieder zur Dashboard Komponente zu navigieren. Der Button "Als PDF exportieren" erstellt eine Pdf Datei die sofort gedownloadet werden kann.
Zur Erstellung der Pdf wird eine fertige JavaScript-Bibliothek JsPdf genutzt. Diese Bibliothek erstellt direkt im Browser eine Pdf, dies hat den Vorteil das kein Backend benötigt wird.

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.7]{pics/examplePdf.png}
    \caption{Door Logs Pdf example}
    \label{fig:LogsPdf}
\end{figure}

\subsubsection{plotly-chart}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.22]{pics/PlotlyChart.png}
    \caption{Plotly Chart Beispiel}
    \label{fig:PlotlyChart}
\end{figure}

Für die Umsetzung der Diagramm-Visualisierung wurde Plotly gewählt und nicht Grafana. Ausschlaggebend war vor allem die bessere Integration in die bestehende Angular-Frontend-Architektur. Bei Grafana wäre ein zusätzlicher Betriebs- und Integrationsaufwand entstanden. Nach zwei Tagen entschied die Firma kein Grafana zu wollen, sondern eine alternative zu bevorzugen. Die Plotly-Chart-Komponente übernimmt die Darstellung von Daten im Dashboard. Sie ist als wiederverwendbare UI-Komponente aufgebaut und erhält ihre Daten über Eingabeparameter, wodurch unterschiedliche Diagrammtypen mit derselben Komponente angezeigt werden können. Die Komponente lädt Plotly zur Laufzeit, erstellt die Darstellung beim ersten Rendern und aktualisiert bestehende Diagramme bei Datenänderungen, ohne die gesamte Ansicht neu aufzubauen. Dadurch bleibt die Darstellung auch bei häufigen Aktualisierungen reaktionsfähig. Kontinuierliche Werte wie Temperatur oder Leistung werden als klassische Linienverläufe dargestellt.

\subsubsection{calendar-editor}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.4]{pics/CalenderEditorcomponent.png}
    \caption{calender-editor Komponente}
    \label{fig:CalenderEditor}
\end{figure}
Die calendar-editor Komponente dient der individuellen Bearbeitung der Betriebszeiten der Container. Durch den potenziellen Schaden, den man damit machen könnte, ist sie als geschützte Route eingebunden und kann nur von Admins aufgerufen werden. Erst nach erfolgreicher Anmeldung wird die Komponente freigegeben.
Beim Öffnen lädt die Komponente bestehende Kalenderdaten über den CalendarService und gibt die Elemente vor. Zurzeit wird  der Standard vier Tage, der Standard fünf Tage Kalender und eine Auswahlmöglichkeit für einen Individuellen Kalender geladen, dies kann jedoch jederzeit erweitert werden. Die Auswahl der Standardkalender wurde in Abstimmung mit dem Unternehmen festgelegt. Für Standorte in Wien und im Burgenland wird standardmäßig ein 4-Tage-Kalender verwendet, in den übrigen Bundesländern ein 5-Tage-Kalender. Diese Zuordnung wird bei der Erstellung eines Containers automatisch gesetzt, sollte man sie im Nachhinein jedoch ändern wollen, sollte das leicht möglich sein. Falls eine Baustelle eine abweichende Regelung benötigt, kann unter Individuell ein Kalender mit ganz anderen Betriebszeiten gespeichert werden.


\subsection{Dependency Injection}
Um die Logik in allen Komponenten nicht immer wieder neu Laden zu müssen benutzt Angular das design Pattern der Dependency Injection.
Man erstellt einen Service und kann dann von allen Codeabschnitten, auf die dort definierten Methoden zugreifen. Dies bietet einige Vorteile.
\begin{itemize}
    \item \textbf{Wartbarkeit} Man vermeidet doppelten Code und kann so ganze Datenabfragen mit nur ein paar Zeilen über die gesamte App und in jeder Komponente ändern.
    \item \textbf{Anpassungsfähigkeit} In jeder neuen Komponente die erstellt wird kann sofort, exakt die selbe Logik verwendet werden wie in dem bestehenden Code.
\end{itemize}
Das erstellen eines solchen Services funktioniert ganz einfach in der Angular CLI.
\begin{lstlisting}[language=bash, caption={erstellen eines Services in der Angular CLI}]
ng generate service NAME
\end{lstlisting}
Daraufhin werden alle benötigten Files automatisch erstellt und man kann sofort anfangen zu implementieren.

\subsubsection{Calendar}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.06]{pics/CalenderService.png}
    \caption{Calender Service}
    \label{fig:CalenderService}
\end{figure}

Der Calendar Service ist die zentrale Schnittstelle für alle Aufrufe ans Backend die den Kalender behandeln. Er stellt den Komponenten ein API fürs Lesen speichern, löschen und aktualisieren von den Kalenderdaten bereit.
Konkret lädt er den individuelle Kalender eines jeden Containers über einen Get Aufruf und gibt so die jeweiligen Arbeitszeiten eines jeden Containers vor. Änderungen daran kann man über den selben Endpunkt posten und über einen delete Löschen.
\begin{itemize}
\item \textbf{get:} https://localhost:53488/api/calendar/{containerId}
\item \textbf{post:} https://localhost:53488/api/calendar/{containerId}, data
\item \textbf{delete} https://localhost:53488/api/calendar/{containerId}
\end{itemize}
Über einen eigenen Endpunkt kann man die Arbeitszeiten eines Gewissen Containers mit dessen Id abrufen.  
Als letztes wird noch ein Patch Aufruf bereitgestellt um die Betriebsurlaube zu managen.
\begin{itemize}
\item \textbf{get:} https://localhost:53488/api/Calendar/should-operate/{containerId}
\item \textbf{patch:} https://localhost:53488/api/Calendar/vacations`, { summer, winter }
\end{itemize}

\subsubsection{Container}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.08]{pics/ContainerService.png}
    \caption{Container Service}
    \label{fig:ContainerService}
\end{figure}
Der Container Service ist Primär zuständig einen Endpunkt zum Laden der ganzen Elemente der start-list Komponente bereit zu stellen. 
\begin{itemize}
\item \textbf{get:} https://localhost:53488/api/Containers
\end{itemize}
Per HTTP-GET bekommt man alle Container und deren Daten. Der Aufruf wird, bei Fehlern, bis zu drei Mal erneut und protokolliert Fehler. Dadurch bleibt die Ladefunktion in der Startansicht stabil.


\subsubsection{Keycloak-auth}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.085]{pics/KecloakService.png}
    \caption{Keycloak Service}
    \label{fig:KeycloakService}
\end{figure}
Der KeycloakAuthService regelt die ganze Benutzerauthentifizierung über Keycloak. 
Um die Oberfläche dynamisch anpassen zu können gibt es zwei Streams die von der UI subscribed werden. Einmal für den Authentifizierungsstatus und einmal für das Management der Rollen.
\begin{itemize}
\item \textbf{isAuthenticated:} this.isAuthenticatedSubject.asObservable();
\item \textbf{userRoles:} this.userRolesSubject.asObservable();
\end{itemize}
Der Service regelt unter anderem den Login indem er zur Keycloak Login Page umleitet und danach automatisch zur ursprünglichen Route zurück.
Beim Logout werden beide Subjects, isAuthenticatedSubject und userRolesSubject gelöscht.


\subsubsection{Plug-controll}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.09]{pics/PlugControllService.png}
    \caption{PlugControl Service}
    \label{fig:PlugControlService}
\end{figure}
In der Frontend Architektur übernimmt der plug-controll-service die Steuerung aller Aktoren. Genauer gesagt Licht, Heizung und Kühlung. 
Ein Post mit containerId und plugType wird an den Endpunkt geschickt. Da alle Typen direkt in einem Endpunkt geregelt sind wäre eine Zukünftige Erweiterung, für zum Beispiel ein zweites Licht, sehr einfach.
Der Request gibt nur zurüpck ob es funktioniert hat oder nicht(boolean:true|false). Beim einem Fehlgeschlagenen versuch wird der Fehler direkt geloggt. Dadurch wird ein möglicher Fall, wie das ausstecken eines Controllers, sauber abgefangen ohne Probleme zu verursachen.
Im Gegensatz zu den anderen Services ist der PlugControllService ein reiner schreib-Service, er kann keine Daten lesen.
\begin{itemize}
\item \textbf{post:} https://localhost:53488/api/PlugControl
\end{itemize}
\subsubsection{Sensor-adapter}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.08]{pics/SensorAdapterService.png}
    \caption{SensorAdapterService Service}
    \label{fig:SensorAdapterService}
\end{figure}
Als letzter Service ist der sensor-adapter Service dazu zuständig mit dem plug-controll die ganze Sensor Logik zu vervollständigen. Der Service versorgt das Dashboard mit den aktuellen und historischen Containerdaten wie Temperatur, Steckdosenstatus und Anwesenheitsdaten. 
Alle Daten werden mithilfe eines FirastoreListeners immer live geladen. Eine Änderung in der Datenbank wird sofort erkannt und alle Daten auf der Webseite werden neu geladen. Diese live Updates können aber über zwei Methoden:startLiveUpdates und stopLiveUpdates, ein und ausgeschaltet werden.
\begin{itemize}
\item \textbf{get:} https://localhost:53488/api/Sensors/container/{containerId}
\end{itemize}
Wie vorher gesagt bietet der Service nicht nur live Daten sondern auch einige Endpunkte um historische Daten für Diagramme zu Laden. Dazu gehören der Temperaturverlauf und der Türkontaktverlauf.  
\begin{itemize}
\item \textbf{get:} https://localhost:53488/api/Sensors/history/temperature/{containerId}/{sensorLocation}?days={days}
\item \textbf{get:} https://localhost:53488/api/Sensors/history/plugs/{containerId}/{plugType}?days={days}
\item \textbf{get:} https://localhost:53488/api/Sensors/history/appearance/{containerId}?days={days}
\item \textbf{get:} https://localhost:53488/api/Sensors/history/door/{containerId}?days={days}
\item \textbf{get:} https://localhost:53488/api/Sensors/history/door/{containerId}?days={days}
\end{itemize}

\subsection{Models}
Models in Angular sind TypeScript Interfaces, die festlegen, wie Daten in der Anwendung aussehen sollen. Komponenten und Services arbeiten dadurch mit einheitlichen, erwartbaren Datenobjekten statt mit unstrukturierten Daten. Das verbessert die Lesbarkeit, reduziert Fehler und macht die Kommunikation einfacher.

\subsubsection{Container Model}
Das Container-Model definiert und beschreibt die vollständige Datenstruktur eines Containers. Alle Stammdaten und Zuordnung, Sensordaten und schafft damit eine typsichere Grundlage für Anzeige, Verarbeitung und Kommunikation.

\begin{lstlisting}[
    caption=Container Model,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]

export interface Container {
  id?: string; 
  name: string;
  constructionSite: string;
  doorId: string;
  insideSensorId: string;
  mainContainer: boolean; 
  outsideSensorId: string;
  plugId: string;
  containerId:number,
  
  temperatureOutside?: number;
  temperatureInside?: number; 
  humidityInside?: number;    
  humidityOutside?: number;   
  doorOpen?: boolean;         
  powerPlugged?: boolean;     
  
  lastUpdated?: Date;         
  isOnline?: boolean;         
}

\end{lstlisting}

\subsubsection{Sensor-Data-Model}
Das Sensor-Data-Model in sensor-data.ts definiert die Struktur der Sensordaten, die vom Backend an das Frontend übertragen werden. Es handelt sich dabei um DTOs. Ziel ist, dass Sensorwerte im Dashboard einheitlich verarbeitet und angezeigt werden können, unabhängig vom konkreten Sensortyp.

\begin{itemize}
\item `ContainerSensorsDTO` vereint alle Informationen eines Containers in einem Objekt (Bewegung, Tür, Steckdose, Innen- und Außensensor).
\begin{lstlisting}[
    caption=ContainerSensorsDTO,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]
export interface ContainerSensorsDTO {
  containerId: number;
  appearance?: AppearanceSensorDTO;
  door?: DoorSensorDTO;
  plugLight?: PlugLightSensorDTO;
  insideSensor?: TemperatureHumiditySensorDTO;
  outsideSensor?: TemperatureHumiditySensorDTO;
}
\end{lstlisting}
\item getrennte Submodels(`AppearanceSensorDTO`, `DoorSensorDTO`, `PlugLightSensorDTO` und `TemperatureHumiditySensorDTO`) geben dann mehr Information zu den jeweiligen Sensoren.
\begin{lstlisting}[
    caption=Beispiel für ein Submodel,
    label=lst:realmconfig,
    basicstyle=\ttfamily\small
]
export interface AppearanceSensorDTO {
  motionState: string;
  presence: boolean;
}
\end{lstlisting}

\end{itemize}



\subsection{Guards}
In Angular wird die Zugriffs Kontrolle auf Routen über Guards geregelt. Guards kontrollieren ob User bestimmte Routen aufrufen oder aber auch verlassen dürfen. Sie kontrollieren die Navigation des User, so das dieser ohne die erforderlichen Rechte nicht auf geschützte Routen kommt. Guards bestehen aus einer einer einzigen Typescript Datei, diese kann einfach, in der Angular CLI, mit dem Befehl 
\begin{lstlisting}[language=bash, caption={erstellen eines Guards in der Angular CLI}]
ng generate guard NAME
\end{lstlisting}
erstellt werden. 

\subsubsection{authGuard}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.058]{pics/AblaufAuthGuard.png}
    \caption{Ablauf der Routensicherung im AuthGuard}
    \label{fig:AblaufRoutenSicherung}
\end{figure}

Der erste implimentierte Guard ist der authGuard. Dieser regelt die Zugriffskontrolle auf alle Routen im Frontend. Bevor man auf eine neue Route wechselt wird im Controller geprüft ob der User das nötige, gültige, Token hat. Sollte dies nicht der Fall sein wird der User zur Anmeldung aufgefordert.
Sollte ein Fehler bei der Anmeldung passieren(zum Beispiel Falsches Passwort) wir zurück zur Startseite navigiert. Somit wird versichert das wirklich nur Geschütze Funktionen nach erfolgreicher Anmeldung bedient werden können.

\subsubsection{adminGuard}
\begin{figure}[H]
    \centering
    \includegraphics[scale=0.09]{pics/AdminGuardAblauf.png}
    \caption{Ablauf der Rollenprüfung im AdminGuard}
    \label{fig:AblaufRollenPrüfung}
\end{figure}

Der adminGuard ist eine eine Erweiterung zum authGuard. Bei den aktuellen Funktionsweisen hat er noch keine richtige Aufgabe und ist nur für mögliche zukünftige Erweiterungen der Firma da.
Im Guard wird auf die Rolle des angemeldeten Users geprüft. Zurzeit gibt es nur die rolle Admin. Sollte es jedoch in Zukunft eine Rolle für Mitarbeiter oder Kunden geben würde man die Autorisierung im AdminGuard leicht erweitern können auch ohne sich davor spezifisches Wissen aneignen zu müssen.



\section{Cronjob}
\setauthor{Elias Mahr}

Um die Container wirklich voll automatisch ein- und ausschalten zu können, kam für uns nur die Verwendung eines Cronjobs auf dem Server in Frage. Im Unterricht hatten wir bereits davon gehört.
Ein Cron-Daemon ist ein Linux-Programm, das meistens Shell Skripte zeitgesteuert auslöst, sogenannte Cronjobs. 
Somit kann man Aufgaben nach einem Zeitplan immer wiederholend ausführen.

\subsection{Syntax}

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.3]{pics/Cronjob.png}
    \caption{Cronjob Syntax}
    \label{fig:CronjobSyntax}
\end{figure}

Jeder Cronjob ist eine Zeile in dem crontab File. Diese Zeile definiert mit der oben gezeigten Syntax, wann und wie oft welches Skript ausgeführt werden soll.
So können auch komplizierte Zeitpläne für die Durchführung der Schaltungen gebaut werden. Mit / können Schritte definiert werden, wie zum Beispiel bei Stunde = /6. Das würde bedeuten, dass das angeführte Skript bzw. Programm jede 6. Stunde ausgeführt wird. 
Mit dem Operator ``-'' können Bereiche definiert werden, z.B. Wochentag = 0-6. Das würde bedeuten, dass das ausgeführte Skript bzw. Programm täglich ausgeführt wird.

\subsection{Umsetzung in der Diplomarbeit}
Grundlage für die Automatisierung zur ein und Ausschaltung ist ein Cronjob, der in einem definierten Intervall(5:00-16:00) ein JavaScript Skript startet.
Dieses Skript prüft für jeden Container, ob laut Kalenderregel (Arbeitszeit, Wochentag, Betriebsurlaub) ein Betrieb vorgesehen ist, und wenn ja wann.
Daraufhin werden abhängig vom Ergebnis die entsprechenden Schaltbefehle ausgeführt(Heizung 30 min vor Start einschalten). Während der Arbeitszeit gibt es noch einen Job, der alle 30 min ein Skript ausführt, dass prüft, ob die Türe offen ist, und wenn ja wie lange. Bei zu langer Öffnung der Türe wird die Heizung/Kühlung auch deaktiviert um Strom zu Sparen. Beides Funktioniert über ein Skript am Server, das die jeweiligen Informationen aus der Datenbank liest und wenn nötig neue Daten in die Datenbank schreibt (zum Beispiel den Status des Heating-Plugs auf ''ON'' anstatt ''OFF'').
Der action-handler erkennt die änderung in der Datenbank und schaltet daraufhin den Plug ein. Das Frontend erkennt ebenfalls durch den FirestoreListener die Änderung in der Datenbank und aktualisiert es in der UI.

\section{Reverse-Proxy [Elias Mahr]}
\setauthor{Elias Mahr}
Ein Reverse-Proxy steht zwischen dem Webserver und dem Internet. Im Grunde fungiert es wie ein Türsteher für den Server. Anfragen werden von außen entgegengenommen und zum richtigen Dienst weitergeleitet(Frontend, Backend, Keycloak,...). Dadurch werden die echten Adressen verschleiert.

\begin{figure}[H]
    \centering
    \includegraphics[scale=0.15]{pics/ReverseProxy.png}
    \caption{Reverse-Proxy (Quelle: \url{https://commons.wikimedia.org/wiki/File:Reverse_proxy_h2g2bob.svg}, Lizenz: CC0)}
    \label{fig:ReverseProxy}
\end{figure}

\subsection{Nutzen in der Diplomarbeit}
Es gibt drei Hauptgründe, weshalb ein Reverse-Proxy eingebaut werden musste.
\begin{itemize}
\item \underline{Keycloaks https Redirects:}
Keycloak verwendet moderne Standards wie OpenID Connect. Diese Standards setzen auch eine HTTPs Redirect URL voraus. Https setzt im Gegensatz zu http die nötigen Sicherheitsstandards, um zum Beispiel Man-in-the-Middle-Angriffe zu verhindern.
\item \underline{Port Chaos:}
Mit vielen verschiedenen Ports, wie 80 für das Frontend, 8080 für Keycloak und 5000 für das Backend wäre alles sehr unübersichtlich. Der User müsste sich 3 URLs merken.
\item \underline{Sicherheit:}
Zur Reduzierung der Angriffsfläche sollten nur zwingend notwendige Ports öffentlich erreichbar sein. Jeder zusätzlich exponierte Dienst erhöht das Risiko potenzieller Angriffe.
Interne Dienste, wie beispielsweise Backend-Services auf Port 5000, sind nicht von außen erreichbar und bleiben dadurch vor direktem Zugriff geschützt. Die Trennung zwischen öffentlich zugänglichen Komponenten und internen Services stellt eine grundlegende Sicherheitsmaßnahme dar.
Auch wenn eine Webapplikation ausschließlich innerhalb des Intranets betrieben wird, ist der Einsatz zusätzlicher Sicherheitsmechanismen sinnvoll. Eine mehrschichtige Sicherheitsstrategie (Defense in Depth) trägt dazu bei, das Gesamtrisiko weiter zu reduzieren.
\end{itemize}

\subsection{Umsetzung}

Nginx ist als eigener Docker Container mit nginx.conf implimentiert. Nur der Port 443 ist öffentlich mit Let's Encrypt-Zertifikaten.
Die Ports werden pfadbasiert weitergeleitet:

\begin{itemize}
\item \underline{http://Keycloak:8080 $\to$ location /auth}
\begin{lstlisting}[language=bash, caption=Nginx Konfiguration /auth, label=lst:nginx:/auth]
        location /auth {
            proxy_pass http://keycloak:8080;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;
            proxy_set_header X-Forwarded-Host $host;
            proxy_set_header X-Forwarded-Port 443;
            proxy_buffer_size 128k;
            proxy_buffers 4 256k;
            proxy_busy_buffers_size 256k;
        }
\end{lstlisting}



\item \underline{http://frontend:80 $\to$ location/}
\begin{lstlisting}[language=bash, caption=Nginx Konfiguration /, label=lst:nginx:/]
        location / {
            proxy_pass http://frontend:80;
        }
\end{lstlisting}
\item \underline{http://backend:5000 $\to$ location /api}
\begin{lstlisting}[language=bash, caption=Nginx Konfiguration /api, label=lst:nginx:/api]
        location /api {
            proxy_pass http://backend:5000;
        }
\end{lstlisting}
\end{itemize}

\section{Backend [Timon Schmalzer]}
\setauthor{Timon Schmalzer}

\subsection{Kalenderlogik und Betriebsentscheidung}

Ein zentraler Teil der Backend-Implementierung ist die Entscheidung, ob ein Container zu einem bestimmten Zeitpunkt betrieben werden soll. Diese Entscheidung wird im Endpunkt \texttt{/api/calendar/should-operate/\{containerId\}} berechnet und berücksichtigt sowohl Standardregeln als auch containerabhängige Ausnahmen.

Der Ablauf erfolgt in mehreren Schritten:

\begin{itemize}
    \item Zuerst wird der angeforderte Container geladen und auf Gültigkeit geprüft.
    \item Danach wird geprüft, ob für diesen Container eine individuelle Kalenderregel vorhanden ist.
    \item Falls keine individuelle Regel existiert, wird auf eine Standardregel nach Bundesland zurückgegriffen.
    \item Anschließend werden Feiertage geladen und mit den aktiven Arbeitszeiten kombiniert.
    \item Abschließend wird mit der Kalenderlogik der boolesche Wert \texttt{shouldOperate} berechnet und als API-Antwort zurückgegeben.
\end{itemize}

Diese Vorgehensweise hat den Vorteil, dass kundenspezifische Sonderfälle direkt abbildbar sind, ohne den allgemeinen Standardbetrieb zu verlieren.

\subsubsection{Implementierter Ablauf im Controller}

Die folgende Implementierung zeigt den Kern der Entscheidungskette im \texttt{CalendarController}. Zuerst wird auf eine individuelle Regel geprüft. Ist keine vorhanden, erfolgt automatisch der Fallback auf den jeweiligen Standardkalender:

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Fallback von individuellem auf Standardkalender,
    label=lst:backend:calendar:fallback,
    basicstyle=\ttfamily\small
]
var container = await _containerRepository.GetContainersWithFullDataAsync();
var c = container.FirstOrDefault(x => x.ContainerId == containerId);
if (c == null)
    return NotFound("Container nicht gefunden");

CalendarRule? rule = null;
string calendarType = "";
var customRule = await new CalendarRepository(_firestoreDb).GetByIdAsync(containerId.ToString());
if (customRule != null && customRule.Workdays != null && customRule.Workdays.Count > 0)
{
    rule = customRule;
    calendarType = "custom";
}
else
{
    calendarType = c.State.Trim().ToLower() == "upper_austria" ? "standard_5days" : "standard_4days";
    rule = await _calendarService.GetRuleForLocationAsync(calendarType);
    if (rule == null)
    return NotFound($"Keine Regel fuer Kalender-Typ '{calendarType}' gefunden");
}
\end{lstlisting}

Nach dem Laden der Regel werden Feiertage ergänzt und anschließend die eigentliche Betriebsentscheidung berechnet. Die Antwort enthält neben dem Ergebnis auch den verwendeten Kalendertyp und weitere Kontextdaten für das Frontend.

\subsubsection{Registrierung der Backend-Komponenten}

Damit der Endpunkt stabil funktioniert, werden die benötigten Komponenten im Backend zentral registriert. Besonders wichtig sind dabei die Repositories, der \texttt{CalendarService} sowie die HTTP-Client-Registrierung für externe Feiertagsabfragen.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Service-Registrierung im Backend,
    label=lst:backend:di:registration,
    basicstyle=\ttfamily\small
]
builder.Services.AddScoped<IContainerRepository, ContainerRepository>();
builder.Services.AddScoped<ContainerRepository>();
builder.Services.AddScoped<LatestSensorDataRepository>();
builder.Services.AddScoped<PlugActionRepository>();
builder.Services.AddScoped<CalendarRepository>();
builder.Services.AddScoped<PlugRepository>();
builder.Services.AddHttpClient<CalendarService>();
\end{lstlisting}

Durch diese Trennung bleibt die Implementierung wartbar: Controller kümmern sich um den API-Fluss, Services um Fachlogik und Repositories um den Zugriff auf gespeicherte Daten.

\subsubsection{Nutzen für das Gesamtsystem}

Die umgesetzte Kalenderlogik ist für CESC besonders relevant, da sich der reale Betrieb der Container nicht nur nach Uhrzeit, sondern auch nach Sonderfällen wie Betriebsurlaub und Feiertagen richtet. Durch die implementierte Fallback-Strategie bleibt das System auch dann funktionsfähig, wenn für einzelne Container keine individuelle Konfiguration hinterlegt ist.

Zusätzlich kann das Frontend mit der Antwortstruktur sofort arbeiten, da alle für die Anzeige und Steuerung nötigen Informationen in einer Antwort zusammengeführt werden.


\subsection{Container- und Sensorendpunkte}

Neben der Kalenderlogik besteht ein wesentlicher Teil des Backends aus Endpunkten zum Abrufen von Container- und Sensordaten. Diese Endpunkte versorgen das Frontend mit den aktuellen Zuständen und den Verlaufsdaten, die in den Dashboards angezeigt werden.

\subsubsection{Containerdaten als Einstiegspunkt}

Die Containerdaten werden im \texttt{ContainersController} bereitgestellt. Dabei gibt es einen Endpunkt für alle Container und einen Endpunkt für einen einzelnen Container.

Die Implementierung ist bewusst als Lesezugriff ausgelegt. Schreiboperationen auf Container wurden nicht freigegeben, da in CESC die Anzeige und Steuerlogik im Vordergrund steht und Containerstammdaten nicht laufend durch Benutzer bearbeitet werden müssen.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Container-Endpunkte im Backend,
    label=lst:backend:containers:endpoints,
    basicstyle=\ttfamily\small
]
[HttpGet]
public async Task<ActionResult<IEnumerable<Container>>> GetContainers()
{
    return Ok(await _containerRepository.GetContainersWithFullDataAsync());
}

[HttpGet("{id}")]
public async Task<ActionResult<Container>> GetContainer(string id)
{
    string formattedId = id;
    if (int.TryParse(id, out int containerId))
    {
        formattedId = $"container_{containerId}";
    }

    var container = await _containerRepository.GetContainerWithFullDataAsync(formattedId);
    if (container == null)
    {
        return NotFound();
    }
    return Ok(container);
}
\end{lstlisting}

Die ID-Formatierung ist hier wichtig, da das Frontend mit numerischen IDs arbeiten kann, während intern mit dem Format \texttt{container\_X} gearbeitet wird.

\subsubsection{Zusammenführung der Sensorsignale}

Im \texttt{SensorsController} werden verschiedene Sensortypen aus der Datenbasis geladen und in ein gemeinsames DTO übersetzt. Dadurch erhält das Frontend eine einheitliche Struktur, obwohl die Rohdaten aus unterschiedlichen Sensorquellen stammen.

Die Zuordnung erfolgt über \texttt{sensorType} und \texttt{sensorId}. Je nach Typ wird der entsprechende Teil des Antwortobjekts gesetzt, zum Beispiel für Temperatur, Türkontakt, Bewegungsmelder oder Steckdosenstatus.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Zuordnung von Sensordaten in ein gemeinsames DTO,
    label=lst:backend:sensors:mapping,
    basicstyle=\ttfamily\small
]
if (sensorType == "th-inside" || sensorId.Contains("th_inside"))
{
    var tempSensor = sensor as TemperatureHumiditySensor;
    if (tempSensor != null)
    {
        result.InsideSensor = new TemperatureHumiditySensorDTO
        {
            Battery = tempSensor.Battery ?? 0,
            LastHumidity = tempSensor.LastHumidity,
            LastTemp = tempSensor.LastTemp
        };
    }
}
else if (sensorType == "th-outside" || sensorId.Contains("th_outside"))
{
    var tempSensor = sensor as TemperatureHumiditySensor;
    if (tempSensor != null)
    {
        result.OutsideSensor = new TemperatureHumiditySensorDTO
        {
            Battery = tempSensor.Battery ?? 0,
            LastHumidity = tempSensor.LastHumidity,
            LastTemp = tempSensor.LastTemp
        };
    }
}
\end{lstlisting}

Dieses Mapping reduziert Komplexität im Frontend deutlich, weil dort nicht mehr jedes Sensorschema einzeln behandelt werden muss.

\subsubsection{Verlaufsdaten für Analysefunktionen}

Für Diagramme und Rückblicke stellt das Backend eigene History-Endpunkte bereit, zum Beispiel für Temperatur, Plug-Zustände, Bewegung und Türkontakte. Die API arbeitet dabei mit einem \texttt{days}-Parameter, über den der Zeitraum dynamisch gesetzt wird.

Vor der eigentlichen Datenabfrage werden die Parameter validiert, damit nur gültige Typen verarbeitet werden. Ungültige Werte werden mit \texttt{BadRequest} abgewiesen.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Parameterprüfung im History-Endpunkt,
    label=lst:backend:sensors:validation,
    basicstyle=\ttfamily\small
]
[HttpGet("history/plugs/{containerId}/{plugType}")]
public async Task<ActionResult> GetPlugDeviceHistory(
    int containerId,
    string plugType,
    [FromQuery] int days = 7)
{
    var validTypes = new[] { "light", "heating", "cooling" };
    if (!validTypes.Contains(plugType.ToLower()))
    {
        return BadRequest("plugType must be 'light', 'heating', or 'cooling'");
    }

    var result = await _sensorRepository.GetPlugDeviceHistory(containerId, days);
    if (result == null)
    {
        return NotFound($"No plug device data found for container {containerId}");
    }

    return Ok(result);
}
\end{lstlisting}

Diese Trennung in aktuelle Werte und Verlaufsdaten ist für CESC besonders sinnvoll: Die Übersicht im Dashboard braucht schnelle Live-Werte, während die Analysefunktionen gezielt historische Daten über einen definierten Zeitraum benötigen.

\subsubsection{Nutzen für den Betrieb}

Durch die klare Aufteilung der Endpunkte ist das Backend sowohl für Echtzeitansichten als auch für Auswertungen geeignet. Gleichzeitig bleibt die Schnittstelle nachvollziehbar, da jede Endpunktgruppe einen klar abgegrenzten Zweck hat.

Für die Firma bedeutet das in der Praxis, dass sowohl der aktuelle Zustand eines Containers als auch dessen Verlauf jederzeit kontrolliert werden kann. Damit wird die technische Grundlage für eine verlässliche Überwachung und spätere Optimierungen im Energieverbrauch geschaffen.


\subsection{Program.cs und Middleware-Pipeline}

Die Datei \texttt{Program.cs} bildet den zentralen Einstiegspunkt des Backends. In ihr werden alle notwendigen Services registriert und die HTTP-Pipeline in der richtigen Reihenfolge aufgebaut. Dadurch wird sichergestellt, dass Authentifizierung, Autorisierung, CORS und Controller-Verarbeitung stabil zusammenspielen.

\subsubsection{Initialisierung und Service-Container}

Zu Beginn wird der ASP.NET-Core-Servicecontainer aufgebaut. Neben den Controllern werden JSON-Optionen, Swagger, Authentifizierung und die eigenen Repository-Klassen registriert. Besonders wichtig ist dabei die klare Trennung zwischen Framework-Services und projektspezifischen Komponenten.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Registrierung zentraler Backend-Services,
    label=lst:backend:program:services,
    basicstyle=\ttfamily\small
]
builder.Services.AddControllers()
    .AddJsonOptions(options =>
    {
        options.JsonSerializerOptions.Converters.Add(new JsonStringEnumConverter());
        options.JsonSerializerOptions.DefaultIgnoreCondition = JsonIgnoreCondition.WhenWritingNull;
    });

builder.Services.AddScoped<IContainerRepository, ContainerRepository>();
builder.Services.AddScoped<ContainerRepository>();
builder.Services.AddScoped<LatestSensorDataRepository>();
builder.Services.AddScoped<PlugActionRepository>();
builder.Services.AddScoped<CalendarRepository>();
builder.Services.AddScoped<PlugRepository>();
builder.Services.AddHttpClient<CalendarService>();
\end{lstlisting}

Durch diese Struktur bleiben Verantwortlichkeiten sauber getrennt: Controller verarbeiten Requests, Repositories übernehmen den Datenzugriff und Services kapseln Fachlogik.

Ein wichtiger Implementierungsaspekt ist die Wahl der Service-Lebensdauer. Repositories werden als \texttt{Scoped} registriert, damit pro HTTP-Anfrage ein konsistenter Arbeitskontext entsteht. Die \texttt{FirestoreDb} wird dagegen als \texttt{Singleton} registriert, da die Datenbankverbindung teuer in der Initialisierung ist und von allen Requests wiederverwendet werden soll.

\subsubsection{Middleware-Reihenfolge und Request-Flow}

Nach der Service-Registrierung wird die Middleware-Pipeline aufgebaut. Die Reihenfolge ist für das Laufzeitverhalten entscheidend. Wenn Authentifizierung und Autorisierung in falscher Reihenfolge oder an falscher Stelle eingebunden werden, funktionieren geschützte Endpunkte nicht korrekt.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=HTTP-Pipeline im Backend,
    label=lst:backend:program:pipeline,
    basicstyle=\ttfamily\small
]
if (app.Environment.IsDevelopment())
{
    app.UseSwagger();
    app.UseSwaggerUI(c =>
    {
        c.SwaggerEndpoint("/swagger/v1/swagger.json", "CESC API v1");
        c.RoutePrefix = string.Empty;
    });
}

if (!builder.Configuration.GetValue<bool>("Docker", false))
{
    app.UseHttpsRedirection();
}

app.UseCors("AllowAngularApp");
app.UseAuthentication();
app.UseAuthorization();
app.MapControllers();
\end{lstlisting}

Der Request-Flow ist damit klar definiert: Zuerst werden CORS-Regeln angewendet, danach erfolgt die Token-Prüfung, anschließend die Rollenprüfung und zuletzt die Übergabe an den passenden Controller-Endpunkt.

Abbildung~\ref{fig:backend:program:pipeline-flow} zeigt den vereinfachten Ablauf der Pipeline aus Sicht einer eingehenden API-Anfrage.

\IfFileExists{pics/program-pipeline-flow.png}{
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pics/program-pipeline-flow.png}
    \caption{Ablauf der Middleware-Pipeline im Backend}
    \label{fig:backend:program:pipeline-flow}
\end{figure}
}{
\noindent\textit{Hinweis: Die Abbildung \texttt{pics/program-pipeline-flow.png} ist noch nicht eingebunden.}
}

\subsubsection{CORS- und Security-Konfiguration}

Für die sichere Kommunikation mit dem Frontend wurde eine eigene CORS-Policy definiert. Es werden nur die im Projekt notwendigen Ursprünge freigegeben, unter anderem lokale Entwicklungsumgebungen und das interne Docker-Frontend.

Zusätzlich wird die API über JWT-Bearer-Authentifizierung geschützt. Die Konfiguration bezieht sich auf Keycloak und setzt die erforderlichen Token-Validierungsparameter:

\begin{lstlisting}[
    language={[Sharp]C},
    caption=JWT-Bearer-Authentifizierung mit Keycloak,
    label=lst:backend:program:jwt-config,
    basicstyle=\ttfamily\small
]
builder.Services.AddAuthentication(JwtBearerDefaults.AuthenticationScheme)
    .AddJwtBearer(options =>
    {
        options.Authority = "http://keycloak:8080/realms/cesc";
        options.Audience = "cesc-backend";
        options.RequireHttpsMetadata = false; // Nur in Entwicklung
        
        options.TokenValidationParameters = new TokenValidationParameters
        {
            ValidateIssuer = true,
            ValidateAudience = false, // Keycloak inkludiert Audience nicht konsistent
            ValidateLifetime = true,
            ValidateIssuerSigningKey = true,
            ClockSkew = TimeSpan.FromMinutes(5)
        };
    });
\end{lstlisting}

In der Token-Validierung werden Aussteller, Ablaufzeit und Signaturschlüssel geprüft. Die Zielgruppenprüfung ist bewusst deaktiviert (\texttt{ValidateAudience = false}), da die Keycloak-Tokens je nach Fluss nicht immer ein konsistentes Audience-Feld enthalten.

Für rollenbasierte Zugriffe ist im Backend eine \texttt{AdminOnly}-Policy hinterlegt. Damit lassen sich schreibende oder administrative Endpunkte gezielt absichern, ohne den Zugriff auf reine Leseendpunkte unnötig einzuschränken.

\subsubsection{Entwicklungs- und Produktionsverhalten}

Die Pipeline unterscheidet klar zwischen Entwicklungs- und Produktionsbetrieb. Swagger und Swagger-UI werden nur in der Entwicklungsumgebung geladen. Dadurch sind die API-Dokumentation und Testoberfläche lokal direkt verfügbar, ohne in produktiven Umgebungen dauerhaft offen betrieben zu werden.

Zusätzlich wird die HTTPS-Weiterleitung abhängig vom Docker-Flag gesteuert. In lokalen Nicht-Docker-Szenarien wird auf HTTPS umgeleitet, während in containerisierten Deployments die TLS-Terminierung über den vorgeschalteten Reverse-Proxy erfolgt.

\subsubsection{Firestore-Initialisierung und Credentials}

Die Firestore-Anbindung wird in \texttt{Program.cs} direkt initialisiert. Dazu wird zuerst der Credential-Pfad aus der Konfiguration gelesen, anschließend die \texttt{project\_id} aus der JSON-Datei extrahiert und die Umgebungsvariable \texttt{GOOGLE\_APPLICATION\_CREDENTIALS} gesetzt.

Danach wird die \texttt{FirestoreDb} als Singleton registriert. Dadurch verwenden alle Repositories dieselbe initialisierte Datenbankinstanz, was das Laufzeitverhalten stabil und effizient macht.

Diese Initialisierung ist ein zentraler Bestandteil der Backend-Implementierung, weil ohne sie weder Kalenderlogik noch Sensor- und Containerendpunkte Daten aus Firestore lesen oder schreiben könnten.

\subsection{PlugControl und Aktorsteuerung}

Die Aktorsteuerung wird im \texttt{PlugControlController} umgesetzt. Das Backend schaltet die Verbraucher nicht direkt, sondern schreibt Steueraufträge in die Firestore-Collection \texttt{actions}. Diese Entkopplung ist für CESC wichtig, weil die physische Schaltung asynchron auf Raspberry-Pi-Seite durch den Action-Handler verarbeitet wird.

Abbildung~\ref{fig:backend:plugcontrol:sequence} zeigt den Ablauf von der API-Anfrage bis zur asynchronen Schaltausführung auf dem Raspberry Pi.

\IfFileExists{pics/plugcontrol-sequence.png}{
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{pics/plugcontrol-sequence.png}
    \caption{Ablauf der PlugControl-Aktorsteuerung}
    \label{fig:backend:plugcontrol:sequence}
\end{figure}
}{
\noindent\textit{Hinweis: Die Abbildung \texttt{pics/plugcontrol-sequence.png} ist noch nicht eingebunden.}
}

\subsubsection{Steuerendpunkte für Licht, Heizung und Kühlung}

Der zentrale Endpunkt ist \texttt{POST /api/PlugControl}. Das Frontend übergibt \texttt{containerId}, \texttt{plugType} und den Zielstatus \texttt{ON/OFF}. Bei gültiger Anfrage schreibt das Backend ein neues Aktionsdokument mit Zeitstempel und \texttt{Processed = false}. Die Antwort ist bewusst kompakt gehalten und liefert ein Erfolgsflag sowie eine kurze Meldung.

Zusätzlich existiert ein Sammelendpunkt \texttt{POST /api/PlugControl/bulk-switch}. Dieser erzeugt Steueraktionen für mehrere Container in einem Aufruf. Dazu werden Plug-Sensoren aus den Collections \texttt{plugCoolingSensors}, \texttt{plugHeatingSensors} und \texttt{plugLightSensors} gelesen, auf die erlaubten Typen normalisiert und anschließend als einzelne Aktionen persistiert.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Erzeugen einer einzelnen Plug-Aktion,
    label=lst:backend:plugcontrol:create-action,
    basicstyle=\ttfamily\small
]
[HttpPost]
public async Task<IActionResult> ControlPlug([FromBody] PlugControlRequest request)
{
    if (request.Status != "ON" && request.Status != "OFF")
    {
        return BadRequest(new { success = false, message = "Status must be ON or OFF" });
    }

    var newAction = new PlugActionDocument
    {
        ContainerId = request.ContainerId,
        PlugType = request.PlugType,
        Status = request.Status,
        Timestamp = DateTime.UtcNow,
        Processed = false
    };

    await db.Collection("actions").AddAsync(newAction);
    return Ok(new { success = true, message = "Action created" });
}
\end{lstlisting}

\subsubsection{Validierung der Steuerbefehle}

Vor dem Schreiben in Firestore werden die Eingaben serverseitig geprüft. Dadurch wird verhindert, dass unvollständige oder fachlich ungültige Steueraufträge in die Datenbasis gelangen. Geprüft werden insbesondere eine gültige \texttt{containerId}, ein gesetzter \texttt{plugType} und ein erlaubter \texttt{status}.

Im Bulk-Endpunkt wird zusätzlich verhindert, dass doppelte Aktionen für denselben Plug erzeugt werden. Dafür wird ein eindeutiger Schlüssel aus \texttt{containerId} und \texttt{sensorId} verwendet.

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Validierung und Dublettenvermeidung beim Bulk-Switch,
    label=lst:backend:plugcontrol:bulk-validation,
    basicstyle=\ttfamily\small
]
if (request.Status != "ON" && request.Status != "OFF")
{
    return BadRequest(new { success = false, message = "Status must be ON or OFF" });
}

var uniquePlugs = new HashSet<string>();
string uniqueKey = $"{containerId}|{sensorId}";
if (uniquePlugs.Contains(uniqueKey)) continue;
uniquePlugs.Add(uniqueKey);
\end{lstlisting}

\subsubsection{Fehlerbehandlung bei Schaltvorgängen}

Die Fehlerbehandlung ist so umgesetzt, dass das Frontend klare Rückmeldungen erhält. Fachliche Validierungsfehler werden als \texttt{400 Bad Request} zurückgegeben. Tritt beim Schreiben in Firestore ein technischer Fehler auf, antwortet der Endpunkt mit \texttt{500 Internal Server Error}.

Zusätzlich protokolliert der Controller alle relevanten Schritte mit \texttt{ILogger}. Dadurch kann im Betrieb nachvollzogen werden, welcher Container, welcher Plug-Typ und welcher Zielzustand angefordert wurde. Diese Logs sind besonders hilfreich, wenn einzelne Schaltvorgänge wegen Verbindungs- oder Datenproblemen nicht ausgeführt werden.

Durch die Kombination aus Validierung, klaren Statuscodes und strukturiertem Logging bleibt die Aktorsteuerung auch bei Fehlerfällen robust und für Wartung sowie Troubleshooting gut nachvollziehbar.

\subsubsection{Asynchrone Verarbeitung auf dem Raspberry Pi}

Nachdem die Steueraktion erfolgreich in Firestore gespeichert wurde, endet die Backend-Antwort an das Frontend. Die physische Schaltung erfolgt dann asynchron durch den Action-Handler auf dem Raspberry Pi.

Der Action-Handler läuft kontinuierlich auf dem Raspberry Pi und überwacht die Firestore-Collection \texttt{actions}. Sobald ein neues Dokument mit \texttt{Processed = false} erkannt wird, liest er den Befehl (\texttt{containerId}, \texttt{plugType}, \texttt{status}) aus und sendet ihn über Zigbee an den entsprechenden Zigbee-Stecker. Nach erfolgreich ausgeführter Schaltung aktualisiert der Handler das Aktionsdokument auf \texttt{Processed = true} und fügt einen \texttt{CompletedAt}-Zeitstempel hinzu.

Diese entkoppelte Architektur bietet mehrere Vorteile: Das Backend antwortet schnell und bleibt unabhängig von der physischen Ausführungsgeschwindigkeit. Falls der Raspberry Pi temporär nicht erreichbar ist, sammeln sich die Aktionen in Firestore und werden beim Neustart automatisch verarbeitet. Gleichzeitig können mehrere Container und Plugs parallel gesteuert werden, ohne dass die Backend-API blockiert.

\subsection{Fehlerbehandlung, Logging und API-Rückgabecodes}

In diesem Abschnitt werden Robustheit und Nachvollziehbarkeit der API-Implementierung zusammengefasst.

\subsubsection{Statuscodestrategie im Backend}

Die API befolgt dabei ein klares Statuscode-Schema, das sich an REST-Standards orientiert. Dies ermöglicht dem Frontend, die Anfrageergebnisse zuverlässig zu interpretieren:

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{5pt}
    \begin{tabularx}{\textwidth}{|>{\centering\arraybackslash}p{2.1cm}|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
        \hline
        \textbf{Code} & \textbf{Bedeutung} & \textbf{Beispiel-Szenario} \\
        \hline
        200 OK & Anfrage erfolgreich verarbeitet & Sensor-Daten gelesen, Plug-Aktion erzeugt \\
        \hline
        400 Bad Request & Fachliche Validierung fehlgeschlagen & Ungültiger \texttt{plugType}, fehlende \texttt{containerId} \\
        \hline
        401 Unauthorized & Authentifizierung erforderlich/fehlgeschlagen & Token abgelaufen, falscher/fehlender Bearer-Token \\
        \hline
        403 Forbidden & Authentifizierung ok, aber keine Berechtigung & Benutzer versucht Admin-Endpunkt ohne admin-Rolle \\
        \hline
        404 Not Found & Ressource existiert nicht & Container-ID nicht in Firestore vorhanden \\
        \hline
        500 Internal Server Error & Technischer Fehler im Backend & Firestore-Verbindungsabbruch, unerwartete Exception \\
        \hline
    \end{tabularx}
    \caption{HTTP-Statuscode-Schema im CESC-Backend}
    \label{tbl:backend:http-statuscodes}
\end{table}

Diese Trennung ist wichtig: \textbf{400 Bad Request} signalisiert Eingabefehler durch den Client und erfordert, Benutzereingaben zu korrigieren. \textbf{401/403} sind Authentifizierungs-/Autorisierungsprobleme, die von der Middleware behandelt werden. \textbf{500} deutet auf einen serverinternen Fehler hin, der Logging und Debugging erfordert.

Abbildung~\ref{fig:backend:error:handling-flow} zeigt visuell, an welchen Punkten im Request-Verarbeitungsfluss welche Fehler auftreten können:

\IfFileExists{pics/error-handling-flow.png}{
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth,height=0.88\textheight,keepaspectratio]{pics/error-handling-flow.png}
    \caption{Fehlerbehandlung und Validierungsfluss -- Wo entstehen welche Statuscodes}
    \label{fig:backend:error:handling-flow}
\end{figure}
}{
\noindent\textit{Hinweis: Die Abbildung \texttt{pics/error-handling-flow.png} ist noch nicht eingebunden.}
}

\subsubsection{Logging in Controllern und Services}

Die Fehlerbehandlung ist eng mit dem Logging verbunden. Für die Authentifizierung werden kritische Events protokolliert. In \texttt{Program.cs} sind Listener für JWT-Validierungsereignisse registriert:

\begin{lstlisting}[
    language={[Sharp]C},
    caption=JWT-Authentication Logging via JwtBearerEvents,
    label=lst:backend:error:jwt-logging,
    basicstyle=\ttfamily\small
]
options.Events = new JwtBearerEvents
{
    OnAuthenticationFailed = context =>
    {
        Console.WriteLine($"[AUTH] Authentication failed: {context.Exception.Message}");
        return Task.CompletedTask;
    },
    OnTokenValidated = context =>
    {
        Console.WriteLine($"[AUTH] Token validated for user: {context.Principal?.Identity?.Name}");
        return Task.CompletedTask;
    }
};
\end{lstlisting}

Dies ermöglicht es, Authentifizierungsfehler direkt nachzuvollziehen. Außerdem werden kritische Initialisierungsschritte geloggt:

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Initialisierungs-Logging für Firestore,
    label=lst:backend:error:firestore-logging,
    basicstyle=\ttfamily\small
]
try
{
    var json = System.IO.File.ReadAllText(credentialsPath);
    using var doc = System.Text.Json.JsonDocument.Parse(json);
    projectId = doc.RootElement.GetProperty("project_id").GetString();
}
catch (Exception ex)
{
    Console.WriteLine($"[ERROR] Failed to read project_id: {ex.Message}");
    throw;
}

var db = FirestoreDb.Create(projectId);
Console.WriteLine($"[Firestore DEBUG] Using Firestore project: {db.ProjectId}");
\end{lstlisting}

In den Controllern werden ähnlich Validierungsfehler und kritische Operationen protokolliert. Das folgende Listing ist ein vereinfachtes Muster zur Veranschaulichung:

\begin{lstlisting}[
    language={[Sharp]C},
    caption=Fehlerbehandlung in Controllern,
    label=lst:backend:error:controller-logging,
    basicstyle=\ttfamily\small
]
if (request.Status != "ON" && request.Status != "OFF")
{
    _logger.LogWarning($"Invalid status '{request.Status}' for plugType '{plugType}'");
    return BadRequest(new { success = false, message = "Status must be ON or OFF" });
}

try
{
    await _repository.CreateAction(newAction);
    _logger.LogInformation($"Action created for containerId={containerId}, plugType={plugType}");
    return Ok(new { success = true });
}
catch (Exception ex)
{
    _logger.LogError($"Firestore operation failed: {ex.Message}");
    return StatusCode(500, new { success = false, message = "Internal server error" });
}
\end{lstlisting}

\subsubsection{Debugging und Nachvollziehbarkeit im Betrieb}

Für die Wartung und Troubleshooting ist strukturiertes Logging unerlässlich. Die Logs sollten mindestens folgende Informationen enthalten:

\begin{itemize}
    \item \textbf{Zeitstempel}: Wann das Event aufgetreten ist
    \item \textbf{Log-Level}: Information, Warning, Error (für Filterung und Priorisierung)
    \item \textbf{Kontext}: User-ID, Betroffene Ressource (Container-ID, Plug-Typ), Request-ID
    \item \textbf{Nachricht}: Verständliche Beschreibung des Problems oder der Aktion
\end{itemize}

Häufige Fehler und deren Lösung:

\textbf{401 Unauthorized:} Das Token ist abgelaufen oder ungültig. Im Log erscheint die Authentication-Failed-Meldung. Lösung: Token erneuert vom Frontend anfordern.

\textbf{403 Forbidden:} Der Benutzer hat die richtige Rolle nicht. Der Log zeigt den geprüften Benutzerkontext. Lösung: Admin-Rechte in Keycloak überprüfen.

\textbf{400 Bad Request:} Die Eingabevalidierung ist fehlgeschlagen. Der Log nennt das ungültige Feld. Lösung: Frontend-Eingabe überprüfen oder Schnittstelle in der Dokumentation nachschlagen.

\textbf{500 Internal Server Error:} Ein technischer Fehler im Backend ist aufgetreten. Der vollständige Stack-Trace muss in den Logs stehen. Lösung: Logs analysieren, Datenbankverbindung prüfen, ggf. Backend neu starten.

In der Produktion sollten Logs zentral gesammelt werden (z.B. über ELK-Stack oder ähnliche Systeme). Dadurch lassen sich Fehler über mehrere Anfragen zeitlich und sachlich korrelieren, was für die Fehlerbehebung immens hilfreich ist.


\section{Datenbank [Timon Schmalzer]}
\setauthor{Timon Schmalzer}

Dieses Kapitel behandelt die konkrete Datenhaltung des Systems auf Basis von Firebase Firestore.

\subsection{Datenmodell in Firestore}

Die fachliche und technische Struktur der gespeicherten Daten wird hier vollständig dargestellt.

\subsubsection{Collection-Struktur und Dokumentaufbau}

Dieser Abschnitt beschreibt den Aufbau der Collections und die Inhalte einzelner Dokumente.

\subsubsection{Container-, Sensor- und Kalenderdaten}

Hier werden die wichtigsten Datentypen und deren Felder im Projektkontext erläutert.

\subsubsection{Beziehungen und IDs im Datenmodell}

Die Verknüpfung von Entitäten über IDs sowie die Zuordnung zwischen den Datensätzen wird hier erklärt.

\subsection{Repository-Umsetzung für Firestore}

Dieses Unterkapitel dokumentiert den konkreten Datenzugriff im Backend über Repository-Klassen.

\subsubsection{ContainerRepository und SensorRepository}

Die Implementierung und Aufgaben dieser beiden zentralen Repositories werden hier ausgearbeitet.

\subsubsection{CalendarRepository und Regelverwaltung}

Die Verwaltung von Kalenderregeln und Ausnahmen wird in diesem Abschnitt beschrieben.

\subsubsection{Lese- und Schreibzugriffe in der Praxis}

Hier werden typische Read- und Write-Abläufe anhand realer Backend-Operationen dargestellt.

\subsection{Datenflüsse und Konsistenz}

Dieser Abschnitt zeigt, wie Daten durch das System laufen und konsistent gehalten werden.

\subsubsection{Von Sensordaten zur persistierten Struktur}

Die Transformationsschritte von eingehenden Sensordaten bis zur Speicherung werden hier nachvollzogen.

\subsubsection{Fallback-Strategien bei fehlenden Daten}

Hier werden die implementierten Fallback-Mechanismen für unvollständige oder fehlende Daten erklärt.

\subsubsection{Konsistenz zwischen API und Datenbank}

Dieser Teil beschreibt, wie API-Rückgaben und persistierte Datenstrukturen aufeinander abgestimmt bleiben.



